// *** WARNING: this file was generated by crd2pulumi. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";

export namespace cilium {
    export namespace v2 {
        export interface CiliumClusterwideEnvoyConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumClusterwideEnvoyConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpec;
        }

        export interface CiliumClusterwideEnvoyConfigSpec {
            /**
             * BackendServices specifies Kubernetes services whose backends
             * are automatically synced to Envoy using EDS.  Traffic for these
             * services is not forwarded to an Envoy listener. This allows an
             * Envoy listener load balance traffic to these backends while
             * normal Cilium service load balancing takes care of balancing
             * traffic for these services at the same time.
             */
            backendServices: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecBackendServices[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecNodeSelector;
            /**
             * Envoy xDS resources, a list of the following Envoy resource types:
             * type.googleapis.com/envoy.config.listener.v3.Listener,
             * type.googleapis.com/envoy.config.route.v3.RouteConfiguration,
             * type.googleapis.com/envoy.config.cluster.v3.Cluster,
             * type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment, and
             * type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret.
             */
            resources: {[key: string]: any}[];
            /**
             * Services specifies Kubernetes services for which traffic is
             * forwarded to an Envoy listener for L7 load balancing. Backends
             * of these services are automatically synced to Envoy usign EDS.
             */
            services: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecServices[];
        }

        export interface CiliumClusterwideEnvoyConfigSpecBackendServices {
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace defaults to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of port numbers, which can be used for filtering in case of underlying
             * is exposing multiple port numbers.
             */
            number: string[];
        }

        export interface CiliumClusterwideEnvoyConfigSpecBackendServicesPatch {
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace defaults to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of port numbers, which can be used for filtering in case of underlying
             * is exposing multiple port numbers.
             */
            number: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If nil, then this config applies to all nodes.
         */
        export interface CiliumClusterwideEnvoyConfigSpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideEnvoyConfigSpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideEnvoyConfigSpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If nil, then this config applies to all nodes.
         */
        export interface CiliumClusterwideEnvoyConfigSpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumClusterwideEnvoyConfigSpecPatch {
            /**
             * BackendServices specifies Kubernetes services whose backends
             * are automatically synced to Envoy using EDS.  Traffic for these
             * services is not forwarded to an Envoy listener. This allows an
             * Envoy listener load balance traffic to these backends while
             * normal Cilium service load balancing takes care of balancing
             * traffic for these services at the same time.
             */
            backendServices: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecBackendServicesPatch[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecNodeSelectorPatch;
            /**
             * Envoy xDS resources, a list of the following Envoy resource types:
             * type.googleapis.com/envoy.config.listener.v3.Listener,
             * type.googleapis.com/envoy.config.route.v3.RouteConfiguration,
             * type.googleapis.com/envoy.config.cluster.v3.Cluster,
             * type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment, and
             * type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret.
             */
            resources: {[key: string]: any}[];
            /**
             * Services specifies Kubernetes services for which traffic is
             * forwarded to an Envoy listener for L7 load balancing. Backends
             * of these services are automatically synced to Envoy usign EDS.
             */
            services: outputs.cilium.v2.CiliumClusterwideEnvoyConfigSpecServicesPatch[];
        }

        export interface CiliumClusterwideEnvoyConfigSpecServices {
            /**
             * Listener specifies the name of the Envoy listener the
             * service traffic is redirected to. The listener must be
             * specified in the Envoy 'resources' of the same
             * CiliumEnvoyConfig.
             *
             * If omitted, the first listener specified in 'resources' is
             * used.
             */
            listener: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace this is overridden to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of service's frontend ports that should be redirected to the Envoy
             * listener. By default all frontend ports of the service are redirected.
             */
            ports: number[];
        }

        export interface CiliumClusterwideEnvoyConfigSpecServicesPatch {
            /**
             * Listener specifies the name of the Envoy listener the
             * service traffic is redirected to. The listener must be
             * specified in the Envoy 'resources' of the same
             * CiliumEnvoyConfig.
             *
             * If omitted, the first listener specified in 'resources' is
             * used.
             */
            listener: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace this is overridden to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of service's frontend ports that should be redirected to the Envoy
             * listener. By default all frontend ports of the service are redirected.
             */
            ports: number[];
        }

        /**
         * CiliumClusterwideNetworkPolicy is a Kubernetes third-party resource with an
         * modified version of CiliumNetworkPolicy which is cluster scoped rather than
         * namespace scoped.
         */
        export interface CiliumClusterwideNetworkPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumClusterwideNetworkPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpec;
            /**
             * Specs is a list of desired Cilium specific rule specification.
             */
            specs: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecs[];
            status: outputs.cilium.v2.CiliumClusterwideNetworkPolicyStatus;
        }

        /**
         * Spec is the desired Cilium specific rule specification.
         */
        export interface CiliumClusterwideNetworkPolicySpec {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgress[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDeny[];
            enableDefaultDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEnableDefaultDeny;
            endpointSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEndpointSelector;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngress[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDeny[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecLabels[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecNodeSelector;
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgress {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressAuthentication;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToFQDNs[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServices[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDeny {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServices[];
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsFieldsPatch[];
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyPatch {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServices {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressDenyToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorPatch;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressIcmpsFieldsPatch[];
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressPatch {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressAuthenticationPatch;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToFQDNsPatch[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumClusterwideNetworkPolicySpecEgressToFQDNs {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        export interface CiliumClusterwideNetworkPolicySpecEgressToFQDNsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPorts {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsPatch {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServices {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecEgressToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressToServicesK8sServiceSelectorPatch;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumClusterwideNetworkPolicySpecEnableDefaultDeny {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumClusterwideNetworkPolicySpecEnableDefaultDenyPatch {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumClusterwideNetworkPolicySpecEndpointSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEndpointSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEndpointSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecEndpointSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumClusterwideNetworkPolicySpecEndpointSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEndpointSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngress {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressAuthentication;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPorts[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDeny {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyToPorts[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsFieldsPatch[];
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyPatch {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPatch[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressIcmpsFieldsPatch[];
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressPatch {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressAuthenticationPatch;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsPatch[];
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPorts {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsPatch {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecIngressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumClusterwideNetworkPolicySpecLabels {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumClusterwideNetworkPolicySpecLabelsPatch {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumClusterwideNetworkPolicySpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumClusterwideNetworkPolicySpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is the desired Cilium specific rule specification.
         */
        export interface CiliumClusterwideNetworkPolicySpecPatch {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressPatch[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEgressDenyPatch[];
            enableDefaultDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEnableDefaultDenyPatch;
            endpointSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecEndpointSelectorPatch;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressPatch[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecIngressDenyPatch[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecLabelsPatch[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecNodeSelectorPatch;
        }

        /**
         * Rule is a policy rule which must be applied to all endpoints which match the
         * labels contained in the endpointSelector
         *
         * Each rule is split into an ingress section which contains all rules
         * applicable at ingress, and an egress section applicable at egress. For rule
         * types such as `L4Rule` and `CIDR` which can be applied at both ingress and
         * egress, both ingress and egress side have to either specifically allow the
         * connection or one side has to be omitted.
         *
         * Either ingress, egress, or both can be provided. If both ingress and egress
         * are omitted, the rule has no effect.
         */
        export interface CiliumClusterwideNetworkPolicySpecs {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgress[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDeny[];
            enableDefaultDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEnableDefaultDeny;
            endpointSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEndpointSelector;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngress[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDeny[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsLabels[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsNodeSelector;
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgress {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressAuthentication;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToFQDNs[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServices[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDeny {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServices[];
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsFieldsPatch[];
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyPatch {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServices {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorPatch;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressIcmpsFieldsPatch[];
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressPatch {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressAuthenticationPatch;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToFQDNsPatch[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumClusterwideNetworkPolicySpecsEgressToFQDNs {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        export interface CiliumClusterwideNetworkPolicySpecsEgressToFQDNsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPorts {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsPatch {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServices {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEgressToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressToServicesK8sServiceSelectorPatch;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEnableDefaultDeny {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEnableDefaultDenyPatch {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEndpointSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEndpointSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEndpointSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEndpointSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumClusterwideNetworkPolicySpecsEndpointSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEndpointSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngress {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressAuthentication;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPorts[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDeny {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyToPorts[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsFieldsPatch[];
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyPatch {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPatch[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromGroups {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressIcmpsFieldsPatch[];
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressPatch {
            authentication: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressAuthenticationPatch;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsPatch[];
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPorts {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsPatch {
            listener: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumClusterwideNetworkPolicySpecsIngressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumClusterwideNetworkPolicySpecsLabels {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumClusterwideNetworkPolicySpecsLabelsPatch {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumClusterwideNetworkPolicySpecsNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumClusterwideNetworkPolicySpecsNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Rule is a policy rule which must be applied to all endpoints which match the
         * labels contained in the endpointSelector
         *
         * Each rule is split into an ingress section which contains all rules
         * applicable at ingress, and an egress section applicable at egress. For rule
         * types such as `L4Rule` and `CIDR` which can be applied at both ingress and
         * egress, both ingress and egress side have to either specifically allow the
         * connection or one side has to be omitted.
         *
         * Either ingress, egress, or both can be provided. If both ingress and egress
         * are omitted, the rule has no effect.
         */
        export interface CiliumClusterwideNetworkPolicySpecsPatch {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressPatch[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEgressDenyPatch[];
            enableDefaultDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEnableDefaultDenyPatch;
            endpointSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsEndpointSelectorPatch;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressPatch[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsIngressDenyPatch[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsLabelsPatch[];
            nodeSelector: outputs.cilium.v2.CiliumClusterwideNetworkPolicySpecsNodeSelectorPatch;
        }

        /**
         * Status is the status of the Cilium policy rule.
         *
         * The reason this field exists in this structure is due a bug in the k8s
         * code-generator that doesn't create a `UpdateStatus` method because the
         * field does not exist in the structure.
         */
        export interface CiliumClusterwideNetworkPolicyStatus {
            conditions: outputs.cilium.v2.CiliumClusterwideNetworkPolicyStatusConditions[];
            /**
             * DerivativePolicies is the status of all policies derived from the Cilium
             * policy
             */
            derivativePolicies: {[key: string]: {[key: string]: string}};
        }

        export interface CiliumClusterwideNetworkPolicyStatusConditions {
            /**
             * The last time the condition transitioned from one status to another.
             */
            lastTransitionTime: string;
            /**
             * A human readable message indicating details about the transition.
             */
            message: string;
            /**
             * The reason for the condition's last transition.
             */
            reason: string;
            /**
             * The status of the condition, one of True, False, or Unknown
             */
            status: string;
            /**
             * The type of the policy condition
             */
            type: string;
        }

        export interface CiliumClusterwideNetworkPolicyStatusConditionsPatch {
            /**
             * The last time the condition transitioned from one status to another.
             */
            lastTransitionTime: string;
            /**
             * A human readable message indicating details about the transition.
             */
            message: string;
            /**
             * The reason for the condition's last transition.
             */
            reason: string;
            /**
             * The status of the condition, one of True, False, or Unknown
             */
            status: string;
            /**
             * The type of the policy condition
             */
            type: string;
        }

        /**
         * Status is the status of the Cilium policy rule.
         *
         * The reason this field exists in this structure is due a bug in the k8s
         * code-generator that doesn't create a `UpdateStatus` method because the
         * field does not exist in the structure.
         */
        export interface CiliumClusterwideNetworkPolicyStatusPatch {
            conditions: outputs.cilium.v2.CiliumClusterwideNetworkPolicyStatusConditionsPatch[];
            /**
             * DerivativePolicies is the status of all policies derived from the Cilium
             * policy
             */
            derivativePolicies: {[key: string]: {[key: string]: string}};
        }

        export interface CiliumEgressGatewayPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumEgressGatewayPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumEgressGatewayPolicySpec;
        }

        export interface CiliumEgressGatewayPolicySpec {
            /**
             * DestinationCIDRs is a list of destination CIDRs for destination IP addresses.
             * If a destination IP matches any one CIDR, it will be selected.
             */
            destinationCIDRs: string[];
            egressGateway: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGateway;
            /**
             * ExcludedCIDRs is a list of destination CIDRs that will be excluded
             * from the egress gateway redirection and SNAT logic.
             * Should be a subset of destinationCIDRs otherwise it will not have any
             * effect.
             */
            excludedCIDRs: string[];
            /**
             * Egress represents a list of rules by which egress traffic is
             * filtered from the source pods.
             */
            selectors: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectors[];
        }

        /**
         * EgressGateway is the gateway node responsible for SNATing traffic.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGateway {
            /**
             * EgressIP is the source IP address that the egress traffic is SNATed
             * with.
             *
             * Example:
             * When set to "192.168.1.100", matching egress traffic will be
             * redirected to the node matching the NodeSelector field and SNATed
             * with IP address 192.168.1.100.
             *
             * When none of the Interface or EgressIP fields is specified, the
             * policy will use the first IPv4 assigned to the interface with the
             * default route.
             */
            egressIP: string;
            /**
             * Interface is the network interface to which the egress IP address
             * that the traffic is SNATed with is assigned.
             *
             * Example:
             * When set to "eth1", matching egress traffic will be redirected to the
             * node matching the NodeSelector field and SNATed with the first IPv4
             * address assigned to the eth1 interface.
             *
             * When none of the Interface or EgressIP fields is specified, the
             * policy will use the first IPv4 assigned to the interface with the
             * default route.
             */
            interface: string;
            nodeSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGatewayNodeSelector;
        }

        /**
         * This is a label selector which selects the node that should act as
         * egress gateway for the given policy.
         * In case multiple nodes are selected, only the first one in the
         * lexical ordering over the node names will be used.
         * This field follows standard label selector semantics.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGatewayNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * This is a label selector which selects the node that should act as
         * egress gateway for the given policy.
         * In case multiple nodes are selected, only the first one in the
         * lexical ordering over the node names will be used.
         * This field follows standard label selector semantics.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EgressGateway is the gateway node responsible for SNATing traffic.
         */
        export interface CiliumEgressGatewayPolicySpecEgressGatewayPatch {
            /**
             * EgressIP is the source IP address that the egress traffic is SNATed
             * with.
             *
             * Example:
             * When set to "192.168.1.100", matching egress traffic will be
             * redirected to the node matching the NodeSelector field and SNATed
             * with IP address 192.168.1.100.
             *
             * When none of the Interface or EgressIP fields is specified, the
             * policy will use the first IPv4 assigned to the interface with the
             * default route.
             */
            egressIP: string;
            /**
             * Interface is the network interface to which the egress IP address
             * that the traffic is SNATed with is assigned.
             *
             * Example:
             * When set to "eth1", matching egress traffic will be redirected to the
             * node matching the NodeSelector field and SNATed with the first IPv4
             * address assigned to the eth1 interface.
             *
             * When none of the Interface or EgressIP fields is specified, the
             * policy will use the first IPv4 assigned to the interface with the
             * default route.
             */
            interface: string;
            nodeSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGatewayNodeSelectorPatch;
        }

        export interface CiliumEgressGatewayPolicySpecPatch {
            /**
             * DestinationCIDRs is a list of destination CIDRs for destination IP addresses.
             * If a destination IP matches any one CIDR, it will be selected.
             */
            destinationCIDRs: string[];
            egressGateway: outputs.cilium.v2.CiliumEgressGatewayPolicySpecEgressGatewayPatch;
            /**
             * ExcludedCIDRs is a list of destination CIDRs that will be excluded
             * from the egress gateway redirection and SNAT logic.
             * Should be a subset of destinationCIDRs otherwise it will not have any
             * effect.
             */
            excludedCIDRs: string[];
            /**
             * Egress represents a list of rules by which egress traffic is
             * filtered from the source pods.
             */
            selectors: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsPatch[];
        }

        export interface CiliumEgressGatewayPolicySpecSelectors {
            namespaceSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNamespaceSelector;
            nodeSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNodeSelector;
            podSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsPodSelector;
        }

        /**
         * Selects Namespaces using cluster-scoped labels. This field follows standard label
         * selector semantics; if present but empty, it selects all namespaces.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNamespaceSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * Selects Namespaces using cluster-scoped labels. This field follows standard label
         * selector semantics; if present but empty, it selects all namespaces.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * This is a label selector which selects Pods by Node. This field follows standard label
         * selector semantics; if present but empty, it selects all nodes.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * This is a label selector which selects Pods by Node. This field follows standard label
         * selector semantics; if present but empty, it selects all nodes.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumEgressGatewayPolicySpecSelectorsPatch {
            namespaceSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNamespaceSelectorPatch;
            nodeSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsNodeSelectorPatch;
            podSelector: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsPodSelectorPatch;
        }

        /**
         * This is a label selector which selects Pods. This field follows standard label
         * selector semantics; if present but empty, it selects all pods.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsPodSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsPodSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsPodSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsPodSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * This is a label selector which selects Pods. This field follows standard label
         * selector semantics; if present but empty, it selects all pods.
         */
        export interface CiliumEgressGatewayPolicySpecSelectorsPodSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEgressGatewayPolicySpecSelectorsPodSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CiliumEndpoint is the status of a Cilium policy rule.
         */
        export interface CiliumEndpoint {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumEndpoint";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            status: outputs.cilium.v2.CiliumEndpointStatus;
        }

        /**
         * EndpointStatus is the status of a Cilium endpoint.
         */
        export interface CiliumEndpointStatus {
            /**
             * Controllers is the list of failing controllers for this endpoint.
             */
            controllers: outputs.cilium.v2.CiliumEndpointStatusControllers[];
            encryption: outputs.cilium.v2.CiliumEndpointStatusEncryption;
            "external-identifiers": outputs.cilium.v2.CiliumEndpointStatusExternalIdentifiers;
            health: outputs.cilium.v2.CiliumEndpointStatusHealth;
            /**
             * ID is the cilium-agent-local ID of the endpoint.
             */
            id: number;
            identity: outputs.cilium.v2.CiliumEndpointStatusIdentity;
            /**
             * Log is the list of the last few warning and error log entries
             */
            log: outputs.cilium.v2.CiliumEndpointStatusLog[];
            /**
             * NamedPorts List of named Layer 4 port and protocol pairs which will be used in Network
             * Policy specs.
             *
             * swagger:model NamedPorts
             */
            "named-ports": outputs.cilium.v2.CiliumEndpointStatusNamedPorts[];
            networking: outputs.cilium.v2.CiliumEndpointStatusNetworking;
            policy: outputs.cilium.v2.CiliumEndpointStatusPolicy;
            /**
             * State is the state of the endpoint.
             */
            state: string;
        }

        /**
         * ControllerStatus is the status of a failing controller.
         */
        export interface CiliumEndpointStatusControllers {
            configuration: outputs.cilium.v2.CiliumEndpointStatusControllersConfiguration;
            /**
             * Name is the name of the controller
             */
            name: string;
            status: outputs.cilium.v2.CiliumEndpointStatusControllersStatus;
            /**
             * UUID is the UUID of the controller
             */
            uuid: string;
        }

        /**
         * Configuration is the controller configuration
         */
        export interface CiliumEndpointStatusControllersConfiguration {
            /**
             * Retry on error
             */
            "error-retry": boolean;
            /**
             * Base error retry back-off time
             * Format: duration
             */
            "error-retry-base": number;
            /**
             * Regular synchronization interval
             * Format: duration
             */
            interval: number;
        }

        /**
         * Configuration is the controller configuration
         */
        export interface CiliumEndpointStatusControllersConfigurationPatch {
            /**
             * Retry on error
             */
            "error-retry": boolean;
            /**
             * Base error retry back-off time
             * Format: duration
             */
            "error-retry-base": number;
            /**
             * Regular synchronization interval
             * Format: duration
             */
            interval: number;
        }

        /**
         * ControllerStatus is the status of a failing controller.
         */
        export interface CiliumEndpointStatusControllersPatch {
            configuration: outputs.cilium.v2.CiliumEndpointStatusControllersConfigurationPatch;
            /**
             * Name is the name of the controller
             */
            name: string;
            status: outputs.cilium.v2.CiliumEndpointStatusControllersStatusPatch;
            /**
             * UUID is the UUID of the controller
             */
            uuid: string;
        }

        /**
         * Status is the status of the controller
         */
        export interface CiliumEndpointStatusControllersStatus {
            "consecutive-failure-count": number;
            "failure-count": number;
            "last-failure-msg": string;
            "last-failure-timestamp": string;
            "last-success-timestamp": string;
            "success-count": number;
        }

        /**
         * Status is the status of the controller
         */
        export interface CiliumEndpointStatusControllersStatusPatch {
            "consecutive-failure-count": number;
            "failure-count": number;
            "last-failure-msg": string;
            "last-failure-timestamp": string;
            "last-success-timestamp": string;
            "success-count": number;
        }

        /**
         * Encryption is the encryption configuration of the node
         */
        export interface CiliumEndpointStatusEncryption {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * Encryption is the encryption configuration of the node
         */
        export interface CiliumEndpointStatusEncryptionPatch {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * ExternalIdentifiers is a set of identifiers to identify the endpoint
         * apart from the pod name. This includes container runtime IDs.
         */
        export interface CiliumEndpointStatusExternalIdentifiers {
            /**
             * ID assigned to this attachment by container runtime
             */
            "cni-attachment-id": string;
            /**
             * ID assigned by container runtime (deprecated, may not be unique)
             */
            "container-id": string;
            /**
             * Name assigned to container (deprecated, may not be unique)
             */
            "container-name": string;
            /**
             * Docker endpoint ID
             */
            "docker-endpoint-id": string;
            /**
             * Docker network ID
             */
            "docker-network-id": string;
            /**
             * K8s namespace for this endpoint (deprecated, may not be unique)
             */
            "k8s-namespace": string;
            /**
             * K8s pod name for this endpoint (deprecated, may not be unique)
             */
            "k8s-pod-name": string;
            /**
             * K8s pod for this endpoint (deprecated, may not be unique)
             */
            "pod-name": string;
        }

        /**
         * ExternalIdentifiers is a set of identifiers to identify the endpoint
         * apart from the pod name. This includes container runtime IDs.
         */
        export interface CiliumEndpointStatusExternalIdentifiersPatch {
            /**
             * ID assigned to this attachment by container runtime
             */
            "cni-attachment-id": string;
            /**
             * ID assigned by container runtime (deprecated, may not be unique)
             */
            "container-id": string;
            /**
             * Name assigned to container (deprecated, may not be unique)
             */
            "container-name": string;
            /**
             * Docker endpoint ID
             */
            "docker-endpoint-id": string;
            /**
             * Docker network ID
             */
            "docker-network-id": string;
            /**
             * K8s namespace for this endpoint (deprecated, may not be unique)
             */
            "k8s-namespace": string;
            /**
             * K8s pod name for this endpoint (deprecated, may not be unique)
             */
            "k8s-pod-name": string;
            /**
             * K8s pod for this endpoint (deprecated, may not be unique)
             */
            "pod-name": string;
        }

        /**
         * Health is the overall endpoint & subcomponent health.
         */
        export interface CiliumEndpointStatusHealth {
            /**
             * bpf
             */
            bpf: string;
            /**
             * Is this endpoint reachable
             */
            connected: boolean;
            /**
             * overall health
             */
            overallHealth: string;
            /**
             * policy
             */
            policy: string;
        }

        /**
         * Health is the overall endpoint & subcomponent health.
         */
        export interface CiliumEndpointStatusHealthPatch {
            /**
             * bpf
             */
            bpf: string;
            /**
             * Is this endpoint reachable
             */
            connected: boolean;
            /**
             * overall health
             */
            overallHealth: string;
            /**
             * policy
             */
            policy: string;
        }

        /**
         * Identity is the security identity associated with the endpoint
         */
        export interface CiliumEndpointStatusIdentity {
            /**
             * ID is the numeric identity of the endpoint
             */
            id: number;
            /**
             * Labels is the list of labels associated with the identity
             */
            labels: string[];
        }

        /**
         * Identity is the security identity associated with the endpoint
         */
        export interface CiliumEndpointStatusIdentityPatch {
            /**
             * ID is the numeric identity of the endpoint
             */
            id: number;
            /**
             * Labels is the list of labels associated with the identity
             */
            labels: string[];
        }

        /**
         * EndpointStatusChange Indication of a change of status
         *
         * swagger:model EndpointStatusChange
         */
        export interface CiliumEndpointStatusLog {
            /**
             * Code indicate type of status change
             * Enum: ["ok","failed"]
             */
            code: string;
            /**
             * Status message
             */
            message: string;
            /**
             * state
             */
            state: string;
            /**
             * Timestamp when status change occurred
             */
            timestamp: string;
        }

        /**
         * EndpointStatusChange Indication of a change of status
         *
         * swagger:model EndpointStatusChange
         */
        export interface CiliumEndpointStatusLogPatch {
            /**
             * Code indicate type of status change
             * Enum: ["ok","failed"]
             */
            code: string;
            /**
             * Status message
             */
            message: string;
            /**
             * state
             */
            state: string;
            /**
             * Timestamp when status change occurred
             */
            timestamp: string;
        }

        /**
         * Port Layer 4 port / protocol pair
         *
         * swagger:model Port
         */
        export interface CiliumEndpointStatusNamedPorts {
            /**
             * Optional layer 4 port name
             */
            name: string;
            /**
             * Layer 4 port number
             */
            port: number;
            /**
             * Layer 4 protocol
             * Enum: ["TCP","UDP","SCTP","ICMP","ICMPV6","ANY"]
             */
            protocol: string;
        }

        /**
         * Port Layer 4 port / protocol pair
         *
         * swagger:model Port
         */
        export interface CiliumEndpointStatusNamedPortsPatch {
            /**
             * Optional layer 4 port name
             */
            name: string;
            /**
             * Layer 4 port number
             */
            port: number;
            /**
             * Layer 4 protocol
             * Enum: ["TCP","UDP","SCTP","ICMP","ICMPV6","ANY"]
             */
            protocol: string;
        }

        /**
         * Networking is the networking properties of the endpoint.
         */
        export interface CiliumEndpointStatusNetworking {
            /**
             * IP4/6 addresses assigned to this Endpoint
             */
            addressing: outputs.cilium.v2.CiliumEndpointStatusNetworkingAddressing[];
            /**
             * NodeIP is the IP of the node the endpoint is running on. The IP must
             * be reachable between nodes.
             */
            node: string;
        }

        /**
         * AddressPair is a pair of IPv4 and/or IPv6 address.
         */
        export interface CiliumEndpointStatusNetworkingAddressing {
            ipv4: string;
            ipv6: string;
        }

        /**
         * AddressPair is a pair of IPv4 and/or IPv6 address.
         */
        export interface CiliumEndpointStatusNetworkingAddressingPatch {
            ipv4: string;
            ipv6: string;
        }

        /**
         * Networking is the networking properties of the endpoint.
         */
        export interface CiliumEndpointStatusNetworkingPatch {
            /**
             * IP4/6 addresses assigned to this Endpoint
             */
            addressing: outputs.cilium.v2.CiliumEndpointStatusNetworkingAddressingPatch[];
            /**
             * NodeIP is the IP of the node the endpoint is running on. The IP must
             * be reachable between nodes.
             */
            node: string;
        }

        /**
         * EndpointStatus is the status of a Cilium endpoint.
         */
        export interface CiliumEndpointStatusPatch {
            /**
             * Controllers is the list of failing controllers for this endpoint.
             */
            controllers: outputs.cilium.v2.CiliumEndpointStatusControllersPatch[];
            encryption: outputs.cilium.v2.CiliumEndpointStatusEncryptionPatch;
            "external-identifiers": outputs.cilium.v2.CiliumEndpointStatusExternalIdentifiersPatch;
            health: outputs.cilium.v2.CiliumEndpointStatusHealthPatch;
            /**
             * ID is the cilium-agent-local ID of the endpoint.
             */
            id: number;
            identity: outputs.cilium.v2.CiliumEndpointStatusIdentityPatch;
            /**
             * Log is the list of the last few warning and error log entries
             */
            log: outputs.cilium.v2.CiliumEndpointStatusLogPatch[];
            /**
             * NamedPorts List of named Layer 4 port and protocol pairs which will be used in Network
             * Policy specs.
             *
             * swagger:model NamedPorts
             */
            "named-ports": outputs.cilium.v2.CiliumEndpointStatusNamedPortsPatch[];
            networking: outputs.cilium.v2.CiliumEndpointStatusNetworkingPatch;
            policy: outputs.cilium.v2.CiliumEndpointStatusPolicyPatch;
            /**
             * State is the state of the endpoint.
             */
            state: string;
        }

        /**
         * EndpointPolicy represents the endpoint's policy by listing all allowed
         * ingress and egress identities in combination with L4 port and protocol.
         */
        export interface CiliumEndpointStatusPolicy {
            egress: outputs.cilium.v2.CiliumEndpointStatusPolicyEgress;
            ingress: outputs.cilium.v2.CiliumEndpointStatusPolicyIngress;
        }

        /**
         * EndpointPolicyDirection is the list of allowed identities per direction.
         */
        export interface CiliumEndpointStatusPolicyEgress {
            /**
             * Deprecated
             */
            adding: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressAdding[];
            /**
             * AllowedIdentityList is a list of IdentityTuples that species peers that are
             * allowed.
             */
            allowed: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressAllowed[];
            /**
             * DenyIdentityList is a list of IdentityTuples that species peers that are
             * denied.
             */
            denied: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressDenied[];
            enforcing: boolean;
            /**
             * Deprecated
             */
            removing: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressRemoving[];
            /**
             * EndpointPolicyState defines the state of the Policy mode: "enforcing", "non-enforcing", "disabled"
             */
            state: string;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressAdding {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressAddingPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressAllowed {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressAllowedPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressDenied {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressDeniedPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * EndpointPolicyDirection is the list of allowed identities per direction.
         */
        export interface CiliumEndpointStatusPolicyEgressPatch {
            /**
             * Deprecated
             */
            adding: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressAddingPatch[];
            /**
             * AllowedIdentityList is a list of IdentityTuples that species peers that are
             * allowed.
             */
            allowed: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressAllowedPatch[];
            /**
             * DenyIdentityList is a list of IdentityTuples that species peers that are
             * denied.
             */
            denied: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressDeniedPatch[];
            enforcing: boolean;
            /**
             * Deprecated
             */
            removing: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressRemovingPatch[];
            /**
             * EndpointPolicyState defines the state of the Policy mode: "enforcing", "non-enforcing", "disabled"
             */
            state: string;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressRemoving {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyEgressRemovingPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * EndpointPolicyDirection is the list of allowed identities per direction.
         */
        export interface CiliumEndpointStatusPolicyIngress {
            /**
             * Deprecated
             */
            adding: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressAdding[];
            /**
             * AllowedIdentityList is a list of IdentityTuples that species peers that are
             * allowed.
             */
            allowed: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressAllowed[];
            /**
             * DenyIdentityList is a list of IdentityTuples that species peers that are
             * denied.
             */
            denied: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressDenied[];
            enforcing: boolean;
            /**
             * Deprecated
             */
            removing: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressRemoving[];
            /**
             * EndpointPolicyState defines the state of the Policy mode: "enforcing", "non-enforcing", "disabled"
             */
            state: string;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressAdding {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressAddingPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressAllowed {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressAllowedPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressDenied {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressDeniedPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * EndpointPolicyDirection is the list of allowed identities per direction.
         */
        export interface CiliumEndpointStatusPolicyIngressPatch {
            /**
             * Deprecated
             */
            adding: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressAddingPatch[];
            /**
             * AllowedIdentityList is a list of IdentityTuples that species peers that are
             * allowed.
             */
            allowed: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressAllowedPatch[];
            /**
             * DenyIdentityList is a list of IdentityTuples that species peers that are
             * denied.
             */
            denied: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressDeniedPatch[];
            enforcing: boolean;
            /**
             * Deprecated
             */
            removing: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressRemovingPatch[];
            /**
             * EndpointPolicyState defines the state of the Policy mode: "enforcing", "non-enforcing", "disabled"
             */
            state: string;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressRemoving {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * IdentityTuple specifies a peer by identity, destination port and protocol.
         */
        export interface CiliumEndpointStatusPolicyIngressRemovingPatch {
            "dest-port": number;
            identity: number;
            "identity-labels": {[key: string]: string};
            protocol: number;
        }

        /**
         * EndpointPolicy represents the endpoint's policy by listing all allowed
         * ingress and egress identities in combination with L4 port and protocol.
         */
        export interface CiliumEndpointStatusPolicyPatch {
            egress: outputs.cilium.v2.CiliumEndpointStatusPolicyEgressPatch;
            ingress: outputs.cilium.v2.CiliumEndpointStatusPolicyIngressPatch;
        }

        export interface CiliumEnvoyConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumEnvoyConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumEnvoyConfigSpec;
        }

        export interface CiliumEnvoyConfigSpec {
            /**
             * BackendServices specifies Kubernetes services whose backends
             * are automatically synced to Envoy using EDS.  Traffic for these
             * services is not forwarded to an Envoy listener. This allows an
             * Envoy listener load balance traffic to these backends while
             * normal Cilium service load balancing takes care of balancing
             * traffic for these services at the same time.
             */
            backendServices: outputs.cilium.v2.CiliumEnvoyConfigSpecBackendServices[];
            nodeSelector: outputs.cilium.v2.CiliumEnvoyConfigSpecNodeSelector;
            /**
             * Envoy xDS resources, a list of the following Envoy resource types:
             * type.googleapis.com/envoy.config.listener.v3.Listener,
             * type.googleapis.com/envoy.config.route.v3.RouteConfiguration,
             * type.googleapis.com/envoy.config.cluster.v3.Cluster,
             * type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment, and
             * type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret.
             */
            resources: {[key: string]: any}[];
            /**
             * Services specifies Kubernetes services for which traffic is
             * forwarded to an Envoy listener for L7 load balancing. Backends
             * of these services are automatically synced to Envoy usign EDS.
             */
            services: outputs.cilium.v2.CiliumEnvoyConfigSpecServices[];
        }

        export interface CiliumEnvoyConfigSpecBackendServices {
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace defaults to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of port numbers, which can be used for filtering in case of underlying
             * is exposing multiple port numbers.
             */
            number: string[];
        }

        export interface CiliumEnvoyConfigSpecBackendServicesPatch {
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace defaults to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of port numbers, which can be used for filtering in case of underlying
             * is exposing multiple port numbers.
             */
            number: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If nil, then this config applies to all nodes.
         */
        export interface CiliumEnvoyConfigSpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEnvoyConfigSpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEnvoyConfigSpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumEnvoyConfigSpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If nil, then this config applies to all nodes.
         */
        export interface CiliumEnvoyConfigSpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumEnvoyConfigSpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumEnvoyConfigSpecPatch {
            /**
             * BackendServices specifies Kubernetes services whose backends
             * are automatically synced to Envoy using EDS.  Traffic for these
             * services is not forwarded to an Envoy listener. This allows an
             * Envoy listener load balance traffic to these backends while
             * normal Cilium service load balancing takes care of balancing
             * traffic for these services at the same time.
             */
            backendServices: outputs.cilium.v2.CiliumEnvoyConfigSpecBackendServicesPatch[];
            nodeSelector: outputs.cilium.v2.CiliumEnvoyConfigSpecNodeSelectorPatch;
            /**
             * Envoy xDS resources, a list of the following Envoy resource types:
             * type.googleapis.com/envoy.config.listener.v3.Listener,
             * type.googleapis.com/envoy.config.route.v3.RouteConfiguration,
             * type.googleapis.com/envoy.config.cluster.v3.Cluster,
             * type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment, and
             * type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret.
             */
            resources: {[key: string]: any}[];
            /**
             * Services specifies Kubernetes services for which traffic is
             * forwarded to an Envoy listener for L7 load balancing. Backends
             * of these services are automatically synced to Envoy usign EDS.
             */
            services: outputs.cilium.v2.CiliumEnvoyConfigSpecServicesPatch[];
        }

        export interface CiliumEnvoyConfigSpecServices {
            /**
             * Listener specifies the name of the Envoy listener the
             * service traffic is redirected to. The listener must be
             * specified in the Envoy 'resources' of the same
             * CiliumEnvoyConfig.
             *
             * If omitted, the first listener specified in 'resources' is
             * used.
             */
            listener: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace this is overridden to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of service's frontend ports that should be redirected to the Envoy
             * listener. By default all frontend ports of the service are redirected.
             */
            ports: number[];
        }

        export interface CiliumEnvoyConfigSpecServicesPatch {
            /**
             * Listener specifies the name of the Envoy listener the
             * service traffic is redirected to. The listener must be
             * specified in the Envoy 'resources' of the same
             * CiliumEnvoyConfig.
             *
             * If omitted, the first listener specified in 'resources' is
             * used.
             */
            listener: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             */
            name: string;
            /**
             * Namespace is the Kubernetes service namespace.
             * In CiliumEnvoyConfig namespace this is overridden to the namespace of the CEC,
             * In CiliumClusterwideEnvoyConfig namespace defaults to "default".
             */
            namespace: string;
            /**
             * Ports is a set of service's frontend ports that should be redirected to the Envoy
             * listener. By default all frontend ports of the service are redirected.
             */
            ports: number[];
        }

        /**
         * CiliumExternalWorkload is a Kubernetes Custom Resource that
         * contains a specification for an external workload that can join the
         * cluster.  The name of the CRD is the FQDN of the external workload,
         * and it needs to match the name in the workload registration. The
         * labels on the CRD object are the labels that will be used to
         * allocate a Cilium Identity for the external workload. If
         * 'io.kubernetes.pod.namespace' or 'io.kubernetes.pod.name' labels
         * are not explicitly specified, they will be defaulted to 'default'
         * and <workload name>, respectively. 'io.cilium.k8s.policy.cluster'
         * will always be defined as the name of the current cluster, which
         * defaults to "default".
         */
        export interface CiliumExternalWorkload {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumExternalWorkload";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumExternalWorkloadSpec;
            status: outputs.cilium.v2.CiliumExternalWorkloadStatus;
        }

        /**
         * Spec is the desired configuration of the external Cilium workload.
         */
        export interface CiliumExternalWorkloadSpec {
            /**
             * IPv4AllocCIDR is the range of IPv4 addresses in the CIDR format that the external workload can
             * use to allocate IP addresses for the tunnel device and the health endpoint.
             */
            "ipv4-alloc-cidr": string;
            /**
             * IPv6AllocCIDR is the range of IPv6 addresses in the CIDR format that the external workload can
             * use to allocate IP addresses for the tunnel device and the health endpoint.
             */
            "ipv6-alloc-cidr": string;
        }

        /**
         * Spec is the desired configuration of the external Cilium workload.
         */
        export interface CiliumExternalWorkloadSpecPatch {
            /**
             * IPv4AllocCIDR is the range of IPv4 addresses in the CIDR format that the external workload can
             * use to allocate IP addresses for the tunnel device and the health endpoint.
             */
            "ipv4-alloc-cidr": string;
            /**
             * IPv6AllocCIDR is the range of IPv6 addresses in the CIDR format that the external workload can
             * use to allocate IP addresses for the tunnel device and the health endpoint.
             */
            "ipv6-alloc-cidr": string;
        }

        /**
         * Status is the most recent status of the external Cilium workload.
         * It is a read-only field.
         */
        export interface CiliumExternalWorkloadStatus {
            /**
             * ID is the numeric identity allocated for the external workload.
             */
            id: number;
            /**
             * IP is the IP address of the workload. Empty if the workload has not registered.
             */
            ip: string;
        }

        /**
         * Status is the most recent status of the external Cilium workload.
         * It is a read-only field.
         */
        export interface CiliumExternalWorkloadStatusPatch {
            /**
             * ID is the numeric identity allocated for the external workload.
             */
            id: number;
            /**
             * IP is the IP address of the workload. Empty if the workload has not registered.
             */
            ip: string;
        }

        /**
         * CiliumIdentity is a CRD that represents an identity managed by Cilium.
         * It is intended as a backing store for identity allocation, acting as the
         * global coordination backend, and can be used in place of a KVStore (such as
         * etcd).
         * The name of the CRD is the numeric identity and the labels on the CRD object
         * are the kubernetes sourced labels seen by cilium. This is currently the
         * only label source possible when running under kubernetes. Non-kubernetes
         * labels are filtered but all labels, from all sources, are places in the
         * SecurityLabels field. These also include the source and are used to define
         * the identity.
         * The labels under metav1.ObjectMeta can be used when searching for
         * CiliumIdentity instances that include particular labels. This can be done
         * with invocations such as:
         *
         * 	kubectl get ciliumid -l 'foo=bar'
         */
        export interface CiliumIdentity {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumIdentity";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            /**
             * SecurityLabels is the source-of-truth set of labels for this identity.
             */
            "security-labels": {[key: string]: string};
        }

        /**
         * CiliumLocalRedirectPolicy is a Kubernetes Custom Resource that contains a
         * specification to redirect traffic locally within a node.
         */
        export interface CiliumLocalRedirectPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumLocalRedirectPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumLocalRedirectPolicySpec;
            status: outputs.cilium.v2.CiliumLocalRedirectPolicyStatus;
        }

        /**
         * Spec is the desired behavior of the local redirect policy.
         */
        export interface CiliumLocalRedirectPolicySpec {
            /**
             * Description can be used by the creator of the policy to describe the
             * purpose of this policy.
             */
            description: string;
            redirectBackend: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackend;
            redirectFrontend: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontend;
            /**
             * SkipRedirectFromBackend indicates whether traffic matching RedirectFrontend
             * from RedirectBackend should skip redirection, and hence the traffic will
             * be forwarded as-is.
             *
             * The default is false which means traffic matching RedirectFrontend will
             * get redirected from all pods, including the RedirectBackend(s).
             *
             * Example: If RedirectFrontend is configured to "169.254.169.254:80" as the traffic
             * that needs to be redirected to backends selected by RedirectBackend, if
             * SkipRedirectFromBackend is set to true, traffic going to "169.254.169.254:80"
             * from such backends will not be redirected back to the backends. Instead,
             * the matched traffic from the backends will be forwarded to the original
             * destination "169.254.169.254:80".
             */
            skipRedirectFromBackend: boolean;
        }

        /**
         * Spec is the desired behavior of the local redirect policy.
         */
        export interface CiliumLocalRedirectPolicySpecPatch {
            /**
             * Description can be used by the creator of the policy to describe the
             * purpose of this policy.
             */
            description: string;
            redirectBackend: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendPatch;
            redirectFrontend: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendPatch;
            /**
             * SkipRedirectFromBackend indicates whether traffic matching RedirectFrontend
             * from RedirectBackend should skip redirection, and hence the traffic will
             * be forwarded as-is.
             *
             * The default is false which means traffic matching RedirectFrontend will
             * get redirected from all pods, including the RedirectBackend(s).
             *
             * Example: If RedirectFrontend is configured to "169.254.169.254:80" as the traffic
             * that needs to be redirected to backends selected by RedirectBackend, if
             * SkipRedirectFromBackend is set to true, traffic going to "169.254.169.254:80"
             * from such backends will not be redirected back to the backends. Instead,
             * the matched traffic from the backends will be forwarded to the original
             * destination "169.254.169.254:80".
             */
            skipRedirectFromBackend: boolean;
        }

        /**
         * RedirectBackend specifies backend configuration to redirect traffic to.
         * It can not be empty.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackend {
            localEndpointSelector: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelector;
            /**
             * ToPorts is a list of L4 ports with protocol of node local pod(s) where traffic
             * is redirected to.
             * When multiple ports are specified, the ports must be named.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendToPorts[];
        }

        /**
         * LocalEndpointSelector selects node local pod(s) where traffic is redirected to.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * LocalEndpointSelector selects node local pod(s) where traffic is redirected to.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * RedirectBackend specifies backend configuration to redirect traffic to.
         * It can not be empty.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendPatch {
            localEndpointSelector: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendLocalEndpointSelectorPatch;
            /**
             * ToPorts is a list of L4 ports with protocol of node local pod(s) where traffic
             * is redirected to.
             * When multiple ports are specified, the ports must be named.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectBackendToPortsPatch[];
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendToPorts {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectBackendToPortsPatch {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * RedirectFrontend specifies frontend configuration to redirect traffic from.
         * It can not be empty.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontend {
            addressMatcher: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcher;
            serviceMatcher: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcher;
        }

        /**
         * AddressMatcher is a tuple {IP, port, protocol} that matches traffic to be
         * redirected.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcher {
            /**
             * IP is a destination ip address for traffic to be redirected.
             *
             * Example:
             * When it is set to "169.254.169.254", traffic destined to
             * "169.254.169.254" is redirected.
             */
            ip: string;
            /**
             * ToPorts is a list of destination L4 ports with protocol for traffic
             * to be redirected.
             * When multiple ports are specified, the ports must be named.
             *
             * Example:
             * When set to Port: "53" and Protocol: UDP, traffic destined to port '53'
             * with UDP protocol is redirected.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherToPorts[];
        }

        /**
         * AddressMatcher is a tuple {IP, port, protocol} that matches traffic to be
         * redirected.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherPatch {
            /**
             * IP is a destination ip address for traffic to be redirected.
             *
             * Example:
             * When it is set to "169.254.169.254", traffic destined to
             * "169.254.169.254" is redirected.
             */
            ip: string;
            /**
             * ToPorts is a list of destination L4 ports with protocol for traffic
             * to be redirected.
             * When multiple ports are specified, the ports must be named.
             *
             * Example:
             * When set to Port: "53" and Protocol: UDP, traffic destined to port '53'
             * with UDP protocol is redirected.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherToPortsPatch[];
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherToPorts {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherToPortsPatch {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * RedirectFrontend specifies frontend configuration to redirect traffic from.
         * It can not be empty.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendPatch {
            addressMatcher: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendAddressMatcherPatch;
            serviceMatcher: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherPatch;
        }

        /**
         * ServiceMatcher specifies Kubernetes service and port that matches
         * traffic to be redirected.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcher {
            /**
             * Namespace is the Kubernetes service namespace.
             * The service namespace must match the namespace of the parent Local
             * Redirect Policy.  For Cluster-wide Local Redirect Policy, this
             * can be any namespace.
             */
            namespace: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             * The service type needs to be ClusterIP.
             *
             * Example:
             * When this field is populated with 'serviceName:myService', all the traffic
             * destined to the cluster IP of this service at the (specified)
             * service port(s) will be redirected.
             */
            serviceName: string;
            /**
             * ToPorts is a list of destination service L4 ports with protocol for
             * traffic to be redirected. If not specified, traffic for all the service
             * ports will be redirected.
             * When multiple ports are specified, the ports must be named.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherToPorts[];
        }

        /**
         * ServiceMatcher specifies Kubernetes service and port that matches
         * traffic to be redirected.
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherPatch {
            /**
             * Namespace is the Kubernetes service namespace.
             * The service namespace must match the namespace of the parent Local
             * Redirect Policy.  For Cluster-wide Local Redirect Policy, this
             * can be any namespace.
             */
            namespace: string;
            /**
             * Name is the name of a destination Kubernetes service that identifies traffic
             * to be redirected.
             * The service type needs to be ClusterIP.
             *
             * Example:
             * When this field is populated with 'serviceName:myService', all the traffic
             * destined to the cluster IP of this service at the (specified)
             * service port(s) will be redirected.
             */
            serviceName: string;
            /**
             * ToPorts is a list of destination service L4 ports with protocol for
             * traffic to be redirected. If not specified, traffic for all the service
             * ports will be redirected.
             * When multiple ports are specified, the ports must be named.
             */
            toPorts: outputs.cilium.v2.CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherToPortsPatch[];
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherToPorts {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * PortInfo specifies L4 port number and name along with the transport protocol
         */
        export interface CiliumLocalRedirectPolicySpecRedirectFrontendServiceMatcherToPortsPatch {
            /**
             * Name is a port name, which must contain at least one [a-z],
             * and may also contain [0-9] and '-' anywhere except adjacent to another
             * '-' or in the beginning or the end.
             */
            name: string;
            /**
             * Port is an L4 port number. The string will be strictly parsed as a single uint16.
             */
            port: string;
            /**
             * Protocol is the L4 protocol.
             * Accepted values: "TCP", "UDP"
             */
            protocol: string;
        }

        /**
         * Status is the most recent status of the local redirect policy.
         * It is a read-only field.
         */
        export interface CiliumLocalRedirectPolicyStatus {
            ok: boolean;
        }

        /**
         * Status is the most recent status of the local redirect policy.
         * It is a read-only field.
         */
        export interface CiliumLocalRedirectPolicyStatusPatch {
            ok: boolean;
        }

        /**
         * CiliumNetworkPolicy is a Kubernetes third-party resource with an extended
         * version of NetworkPolicy.
         */
        export interface CiliumNetworkPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumNetworkPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumNetworkPolicySpec;
            /**
             * Specs is a list of desired Cilium specific rule specification.
             */
            specs: outputs.cilium.v2.CiliumNetworkPolicySpecs[];
            status: outputs.cilium.v2.CiliumNetworkPolicyStatus;
        }

        /**
         * Spec is the desired Cilium specific rule specification.
         */
        export interface CiliumNetworkPolicySpec {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumNetworkPolicySpecEgress[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDeny[];
            enableDefaultDeny: outputs.cilium.v2.CiliumNetworkPolicySpecEnableDefaultDeny;
            endpointSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEndpointSelector;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumNetworkPolicySpecIngress[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDeny[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumNetworkPolicySpecLabels[];
            nodeSelector: outputs.cilium.v2.CiliumNetworkPolicySpecNodeSelector;
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecEgress {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecEgressAuthentication;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecEgressIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToFQDNs[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServices[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecEgressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecEgressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecEgressDeny {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServices[];
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecEgressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecEgressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecEgressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecEgressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyIcmpsFieldsPatch[];
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecEgressDenyPatch {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecEgressDenyToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecEgressDenyToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecEgressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecEgressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServices {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecEgressDenyToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyToServicesK8sServiceSelectorPatch;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecEgressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecEgressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecEgressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecEgressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecEgressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecEgressIcmpsFieldsPatch[];
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecEgressPatch {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecEgressAuthenticationPatch;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecEgressIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToFQDNsPatch[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecEgressToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumNetworkPolicySpecEgressToFQDNs {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        export interface CiliumNetworkPolicySpecEgressToFQDNsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecEgressToGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecEgressToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecEgressToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecEgressToGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecEgressToPorts {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsPatch {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecEgressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecEgressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecEgressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecEgressToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecEgressToServices {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecEgressToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEgressToServicesK8sServiceSelectorPatch;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumNetworkPolicySpecEnableDefaultDeny {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumNetworkPolicySpecEnableDefaultDenyPatch {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumNetworkPolicySpecEndpointSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEndpointSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEndpointSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecEndpointSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumNetworkPolicySpecEndpointSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecEndpointSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecIngress {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecIngressAuthentication;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecIngressIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPorts[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecIngressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecIngressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecIngressDeny {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyToPorts[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressDenyFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecIngressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecIngressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecIngressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecIngressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyIcmpsFieldsPatch[];
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecIngressDenyPatch {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyToPortsPatch[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecIngressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecIngressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecIngressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecIngressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecIngressFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecIngressFromGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecIngressFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecIngressFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecIngressFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecIngressFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecIngressFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecIngressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecIngressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecIngressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecIngressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecIngressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecIngressIcmpsFieldsPatch[];
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecIngressPatch {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecIngressAuthenticationPatch;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecIngressFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecIngressIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsPatch[];
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecIngressToPorts {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsPatch {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecIngressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecIngressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecIngressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecIngressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumNetworkPolicySpecLabels {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumNetworkPolicySpecLabelsPatch {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumNetworkPolicySpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumNetworkPolicySpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is the desired Cilium specific rule specification.
         */
        export interface CiliumNetworkPolicySpecPatch {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumNetworkPolicySpecEgressPatch[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecEgressDenyPatch[];
            enableDefaultDeny: outputs.cilium.v2.CiliumNetworkPolicySpecEnableDefaultDenyPatch;
            endpointSelector: outputs.cilium.v2.CiliumNetworkPolicySpecEndpointSelectorPatch;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumNetworkPolicySpecIngressPatch[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecIngressDenyPatch[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumNetworkPolicySpecLabelsPatch[];
            nodeSelector: outputs.cilium.v2.CiliumNetworkPolicySpecNodeSelectorPatch;
        }

        /**
         * Rule is a policy rule which must be applied to all endpoints which match the
         * labels contained in the endpointSelector
         *
         * Each rule is split into an ingress section which contains all rules
         * applicable at ingress, and an egress section applicable at egress. For rule
         * types such as `L4Rule` and `CIDR` which can be applied at both ingress and
         * egress, both ingress and egress side have to either specifically allow the
         * connection or one side has to be omitted.
         *
         * Either ingress, egress, or both can be provided. If both ingress and egress
         * are omitted, the rule has no effect.
         */
        export interface CiliumNetworkPolicySpecs {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumNetworkPolicySpecsEgress[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDeny[];
            enableDefaultDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsEnableDefaultDeny;
            endpointSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEndpointSelector;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumNetworkPolicySpecsIngress[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDeny[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumNetworkPolicySpecsLabels[];
            nodeSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsNodeSelector;
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecsEgress {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressAuthentication;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToFQDNs[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServices[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecsEgressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecsEgressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecsEgressDeny {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyIcmps[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSet[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToEndpoints[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToGroups[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToNodes[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToPorts[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToRequires[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServices[];
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyIcmpsFieldsPatch[];
        }

        /**
         * EgressDenyRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyPatch {
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is not allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to connect
             * to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is not allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServices {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecsEgressDenyToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyToServicesK8sServiceSelectorPatch;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsEgressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsEgressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsEgressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsEgressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressIcmpsFieldsPatch[];
        }

        /**
         * EgressRule contains all rule types which can be applied at egress, i.e.
         * network traffic that originates inside the endpoint and exits the endpoint
         * selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members of the structure are specified, then all members
         *     must match in order for the rule to take effect. The exception to this
         *     rule is the ToRequires member; the effects of any Requires field in any
         *     rule will apply to all other rules as well.
         *
         *   - ToEndpoints, ToCIDR, ToCIDRSet, ToEntities, ToServices and ToGroups are
         *     mutually exclusive. Only one of these members may be present within an
         *     individual rule.
         */
        export interface CiliumNetworkPolicySpecsEgressPatch {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressAuthenticationPatch;
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to connect to.
             *
             * Example:
             * Any endpoint with the label "app=httpd" is allowed to initiate
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressIcmpsPatch[];
            /**
             * ToCIDR is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections. Only connections destined for
             * outside of the cluster and not targeting the host will be subject
             * to CIDR rules.  This will match on the destination IP address of
             * outgoing connections. Adding a prefix into ToCIDR or into ToCIDRSet
             * with no ExcludeCIDRs is equivalent. Overlaps are allowed between
             * ToCIDR and ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24
             */
            toCIDR: string[];
            /**
             * ToCIDRSet is a list of IP blocks which the endpoint subject to the rule
             * is allowed to initiate connections to in addition to connections
             * which are allowed via ToEndpoints, along with a list of subnets contained
             * within their corresponding IP block to which traffic should not be
             * allowed. This will match on the destination IP address of outgoing
             * connections. Adding a prefix into ToCIDR or into ToCIDRSet with no
             * ExcludeCIDRs is equivalent. Overlaps are allowed between ToCIDR and
             * ToCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=database-proxy" is allowed to
             * initiate connections to 10.2.3.0/24 except from IPs in subnet 10.2.3.0/28.
             */
            toCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSetPatch[];
            /**
             * ToEndpoints is a list of endpoints identified by an EndpointSelector to
             * which the endpoints subject to the rule are allowed to communicate.
             *
             * Example:
             * Any endpoint with the label "role=frontend" can communicate with any
             * endpoint carrying the label "role=backend".
             */
            toEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToEndpointsPatch[];
            /**
             * ToEntities is a list of special entities to which the endpoint subject
             * to the rule is allowed to initiate connections. Supported entities are
             * `world`, `cluster`,`host`,`remote-node`,`kube-apiserver`, `init`,
             * `health`,`unmanaged` and `all`.
             */
            toEntities: string[];
            /**
             * ToFQDN allows whitelisting DNS names in place of IPs. The IPs that result
             * from DNS resolution of `ToFQDN.MatchName`s are added to the same
             * EgressRule object as ToCIDRSet entries, and behave accordingly. Any L4 and
             * L7 rules within this EgressRule will also apply to these IPs.
             * The DNS -> IP mapping is re-resolved periodically from within the
             * cilium-agent, and the IPs in the DNS response are effected in the policy
             * for selected pods as-is (i.e. the list of IPs is not modified in any way).
             * Note: An explicit rule to allow for DNS traffic is needed for the pods, as
             * ToFQDN counts as an egress rule and will enforce egress policy when
             * PolicyEnforcment=default.
             * Note: If the resolved IPs are IPs within the kubernetes cluster, the
             * ToFQDN rule will not apply to that IP.
             * Note: ToFQDN cannot occur in the same policy as other To* rules.
             */
            toFQDNs: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToFQDNsPatch[];
            /**
             * ToGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * toGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            toGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToGroupsPatch[];
            /**
             * ToNodes is a list of nodes identified by an
             * EndpointSelector to which endpoints subject to the rule is allowed to communicate.
             */
            toNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToNodesPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * connect to.
             *
             * Example:
             * Any endpoint with the label "role=frontend" is allowed to initiate
             * connections to destination port 8080/tcp
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsPatch[];
            /**
             * ToRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be able to connect to other
             * endpoints. These additional constraints do no by itself grant access
             * privileges and must always be accompanied with at least one matching
             * ToEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires any endpoint to which it
             * communicates to also carry the label "team=A".
             */
            toRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToRequiresPatch[];
            /**
             * ToServices is a list of services to which the endpoint subject
             * to the rule is allowed to initiate connections.
             * Currently Cilium only supports toServices for K8s services.
             */
            toServices: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesPatch[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsEgressToCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        export interface CiliumNetworkPolicySpecsEgressToFQDNs {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        export interface CiliumNetworkPolicySpecsEgressToFQDNsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsEgressToGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsEgressToGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsEgressToGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsEgressToGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecsEgressToPorts {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsPatch {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsEgressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsEgressToRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecsEgressToServices {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sService;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelector;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sService {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sService selects service by name and namespace pair
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServicePatch {
            namespace: string;
            serviceName: string;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelector {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelector;
        }

        /**
         * K8sServiceSelector selects services by k8s labels and namespace
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorPatch {
            namespace: string;
            selector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorPatch;
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector is a label selector for k8s services
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service selects policy targets that are bundled as part of a
         * logical load-balanced service.
         *
         * Currently only Kubernetes-based Services are supported.
         */
        export interface CiliumNetworkPolicySpecsEgressToServicesPatch {
            k8sService: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServicePatch;
            k8sServiceSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressToServicesK8sServiceSelectorPatch;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumNetworkPolicySpecsEnableDefaultDeny {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EnableDefaultDeny determines whether this policy configures the
         * subject endpoint(s) to have a default deny mode. If enabled,
         * this causes all traffic not explicitly allowed by a network policy
         * to be dropped.
         *
         * If not specified, the default is true for each traffic direction
         * that has rules, and false otherwise. For example, if a policy
         * only has Ingress or IngressDeny rules, then the default for
         * ingress is true and egress is false.
         *
         * If multiple policies apply to an endpoint, that endpoint's default deny
         * will be enabled if any policy requests it.
         *
         * This is useful for creating broad-based network policies that will not
         * cause endpoints to enter default-deny mode.
         */
        export interface CiliumNetworkPolicySpecsEnableDefaultDenyPatch {
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to egress traffic.
             */
            egress: boolean;
            /**
             * Whether or not the endpoint should have a default-deny rule applied
             * to ingress traffic.
             */
            ingress: boolean;
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumNetworkPolicySpecsEndpointSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEndpointSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEndpointSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsEndpointSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector selects all endpoints which should be subject to
         * this rule. EndpointSelector and NodeSelector cannot be both empty and
         * are mutually exclusive.
         */
        export interface CiliumNetworkPolicySpecsEndpointSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsEndpointSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecsIngress {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressAuthentication;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPorts[];
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecsIngressAuthentication {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * Authentication is the required authentication type for the allowed traffic, if any.
         */
        export interface CiliumNetworkPolicySpecsIngressAuthenticationPatch {
            /**
             * Mode is the required authentication mode for the allowed traffic, if any.
             */
            mode: string;
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecsIngressDeny {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSet[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromEndpoints[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromGroups[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromNodes[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromRequires[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyIcmps[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyToPorts[];
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyIcmpsFieldsPatch[];
        }

        /**
         * IngressDenyRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet, FromGroups and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyPatch {
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is not allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can not accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyToPortsPatch[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyToPorts {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyToPortsPorts[];
        }

        /**
         * PortDenyRule is a list of ports/protocol that should be used for deny
         * policies. This structure lacks the L7Rules since it's not supported in deny
         * policies.
         */
        export interface CiliumNetworkPolicySpecsIngressDenyToPortsPatch {
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyToPortsPortsPatch[];
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsIngressDenyToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsIngressDenyToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSet {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelector;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * CIDRGroupSelector selects CiliumCIDRGroups by their labels,
         * rather than by name.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CIDRRule is a rule that specifies a CIDR prefix to/from which outside
         * communication  is allowed, along with an optional list of subnets within that
         * CIDR prefix to/from which outside communication is not allowed.
         */
        export interface CiliumNetworkPolicySpecsIngressFromCIDRSetPatch {
            /**
             * CIDR is a CIDR prefix / IP Block.
             */
            cidr: string;
            /**
             * CIDRGroupRef is a reference to a CiliumCIDRGroup object.
             * A CiliumCIDRGroup contains a list of CIDRs that the endpoint, subject to
             * the rule, can (Ingress/Egress) or cannot (IngressDeny/EgressDeny) receive
             * connections from.
             */
            cidrGroupRef: string;
            cidrGroupSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSetCidrGroupSelectorPatch;
            /**
             * ExceptCIDRs is a list of IP blocks which the endpoint subject to the rule
             * is not allowed to initiate connections to. These CIDR prefixes should be
             * contained within Cidr, using ExceptCIDRs together with CIDRGroupRef is not
             * supported yet.
             * These exceptions are only applied to the Cidr in this CIDRRule, and do not
             * apply to any other CIDR prefixes in any other CIDRRules.
             */
            except: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromEndpoints {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromEndpointsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromEndpointsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromEndpointsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromEndpointsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromEndpointsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsIngressFromGroups {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromGroupsAws;
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsIngressFromGroupsAws {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * AWSGroup is an structure that can be used to whitelisting information from AWS integration
         */
        export interface CiliumNetworkPolicySpecsIngressFromGroupsAwsPatch {
            labels: {[key: string]: string};
            region: string;
            securityGroupsIds: string[];
            securityGroupsNames: string[];
        }

        /**
         * Groups structure to store all kinds of new integrations that needs a new
         * derivative policy.
         */
        export interface CiliumNetworkPolicySpecsIngressFromGroupsPatch {
            aws: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromGroupsAwsPatch;
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromNodes {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromNodesMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromNodesMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromNodesMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromNodesPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromNodesMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromRequires {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromRequiresMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromRequiresMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsIngressFromRequiresMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * EndpointSelector is a wrapper for k8s LabelSelector.
         */
        export interface CiliumNetworkPolicySpecsIngressFromRequiresPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromRequiresMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsIngressIcmps {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressIcmpsFields[];
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsIngressIcmpsFields {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPField is a ICMP field.
         */
        export interface CiliumNetworkPolicySpecsIngressIcmpsFieldsPatch {
            /**
             * Family is a IP address version.
             * Currently, we support `IPv4` and `IPv6`.
             * `IPv4` is set as default.
             */
            family: string;
            /**
             * Type is a ICMP-type.
             * It should be an 8bit code (0-255), or it's CamelCase name (for example, "EchoReply").
             * Allowed ICMP types are:
             *     Ipv4: EchoReply | DestinationUnreachable | Redirect | Echo | EchoRequest |
             * 		     RouterAdvertisement | RouterSelection | TimeExceeded | ParameterProblem |
             * 			 Timestamp | TimestampReply | Photuris | ExtendedEcho Request | ExtendedEcho Reply
             *     Ipv6: DestinationUnreachable | PacketTooBig | TimeExceeded | ParameterProblem |
             * 			 EchoRequest | EchoReply | MulticastListenerQuery| MulticastListenerReport |
             * 			 MulticastListenerDone | RouterSolicitation | RouterAdvertisement | NeighborSolicitation |
             * 			 NeighborAdvertisement | RedirectMessage | RouterRenumbering | ICMPNodeInformationQuery |
             * 			 ICMPNodeInformationResponse | InverseNeighborDiscoverySolicitation | InverseNeighborDiscoveryAdvertisement |
             * 			 HomeAgentAddressDiscoveryRequest | HomeAgentAddressDiscoveryReply | MobilePrefixSolicitation |
             * 			 MobilePrefixAdvertisement | DuplicateAddressRequestCodeSuffix | DuplicateAddressConfirmationCodeSuffix |
             * 			 ExtendedEchoRequest | ExtendedEchoReply
             */
            type: number | string;
        }

        /**
         * ICMPRule is a list of ICMP fields.
         */
        export interface CiliumNetworkPolicySpecsIngressIcmpsPatch {
            /**
             * Fields is a list of ICMP fields.
             */
            fields: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressIcmpsFieldsPatch[];
        }

        /**
         * IngressRule contains all rule types which can be applied at ingress,
         * i.e. network traffic that originates outside of the endpoint and
         * is entering the endpoint selected by the endpointSelector.
         *
         *   - All members of this structure are optional. If omitted or empty, the
         *     member will have no effect on the rule.
         *
         *   - If multiple members are set, all of them need to match in order for
         *     the rule to take effect. The exception to this rule is FromRequires field;
         *     the effects of any Requires field in any rule will apply to all other
         *     rules as well.
         *
         *   - FromEndpoints, FromCIDR, FromCIDRSet and FromEntities are mutually
         *     exclusive. Only one of these members may be present within an individual
         *     rule.
         */
        export interface CiliumNetworkPolicySpecsIngressPatch {
            authentication: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressAuthenticationPatch;
            /**
             * FromCIDR is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from. Only connections which
             * do *not* originate from the cluster or from the local host are subject
             * to CIDR rules. In order to allow in-cluster connectivity, use the
             * FromEndpoints field.  This will match on the source IP address of
             * incoming connections. Adding  a prefix into FromCIDR or into
             * FromCIDRSet with no ExcludeCIDRs is  equivalent.  Overlaps are
             * allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.3.9.1
             */
            fromCIDR: string[];
            /**
             * FromCIDRSet is a list of IP blocks which the endpoint subject to the
             * rule is allowed to receive connections from in addition to FromEndpoints,
             * along with a list of subnets contained within their corresponding IP block
             * from which traffic should not be allowed.
             * This will match on the source IP address of incoming connections. Adding
             * a prefix into FromCIDR or into FromCIDRSet with no ExcludeCIDRs is
             * equivalent. Overlaps are allowed between FromCIDR and FromCIDRSet.
             *
             * Example:
             * Any endpoint with the label "app=my-legacy-pet" is allowed to receive
             * connections from 10.0.0.0/8 except from IPs in subnet 10.96.0.0/12.
             */
            fromCIDRSet: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromCIDRSetPatch[];
            /**
             * FromEndpoints is a list of endpoints identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             *
             * Example:
             * Any endpoint with the label "role=backend" can be consumed by any
             * endpoint carrying the label "role=frontend".
             */
            fromEndpoints: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromEndpointsPatch[];
            /**
             * FromEntities is a list of special entities which the endpoint subject
             * to the rule is allowed to receive connections from. Supported entities are
             * `world`, `cluster` and `host`
             */
            fromEntities: string[];
            /**
             * FromGroups is a directive that allows the integration with multiple outside
             * providers. Currently, only AWS is supported, and the rule can select by
             * multiple sub directives:
             *
             * Example:
             * FromGroups:
             * - aws:
             *     securityGroupsIds:
             *     - 'sg-XXXXXXXXXXXXX'
             */
            fromGroups: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromGroupsPatch[];
            /**
             * FromNodes is a list of nodes identified by an
             * EndpointSelector which are allowed to communicate with the endpoint
             * subject to the rule.
             */
            fromNodes: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromNodesPatch[];
            /**
             * FromRequires is a list of additional constraints which must be met
             * in order for the selected endpoints to be reachable. These
             * additional constraints do no by itself grant access privileges and
             * must always be accompanied with at least one matching FromEndpoints.
             *
             * Example:
             * Any Endpoint with the label "team=A" requires consuming endpoint
             * to also carry the label "team=A".
             */
            fromRequires: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressFromRequiresPatch[];
            /**
             * ICMPs is a list of ICMP rule identified by type number
             * which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * type 8 ICMP connections.
             */
            icmps: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressIcmpsPatch[];
            /**
             * ToPorts is a list of destination ports identified by port number and
             * protocol which the endpoint subject to the rule is allowed to
             * receive connections on.
             *
             * Example:
             * Any endpoint with the label "app=httpd" can only accept incoming
             * connections on port 80/tcp.
             */
            toPorts: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsPatch[];
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecsIngressToPorts {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsListener;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsOriginatingTLS;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsPorts[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRules;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsTerminatingTLS;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsListener {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsListenerEnvoyConfig;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsListenerEnvoyConfig {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * EnvoyConfig is a reference to the CEC or CCEC resource in which
         * the listener is defined.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsListenerEnvoyConfigPatch {
            /**
             * Kind is the resource type being referred to. Defaults to CiliumEnvoyConfig or
             * CiliumClusterwideEnvoyConfig for CiliumNetworkPolicy and CiliumClusterwideNetworkPolicy,
             * respectively. The only case this is currently explicitly needed is when referring to a
             * CiliumClusterwideEnvoyConfig from CiliumNetworkPolicy, as using a namespaced listener
             * from a cluster scoped policy is not allowed.
             */
            kind: string;
            /**
             * Name is the resource name of the CiliumEnvoyConfig or CiliumClusterwideEnvoyConfig where
             * the listener is defined in.
             */
            name: string;
        }

        /**
         * listener specifies the name of a custom Envoy listener to which this traffic should be
         * redirected to.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsListenerPatch {
            envoyConfig: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsListenerEnvoyConfigPatch;
            /**
             * Name is the name of the listener.
             */
            name: string;
            /**
             * Priority for this Listener that is used when multiple rules would apply different
             * listeners to a policy map entry. Behavior of this is implementation dependent.
             */
            priority: number;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsOriginatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * OriginatingTLS is the TLS context for the connections originated by
         * the L7 proxy.  For egress policy this specifies the client-side TLS
         * parameters for the upstream connection originating from the L7 proxy
         * to the remote destination. For ingress policy this specifies the
         * client-side TLS parameters for the connection from the L7 proxy to
         * the local endpoint.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRule is a list of ports/protocol combinations with optional Layer 7
         * rules which must be met.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsPatch {
            listener: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsListenerPatch;
            originatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsOriginatingTLSPatch;
            /**
             * Ports is a list of L4 port/protocol
             */
            ports: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsPortsPatch[];
            rules: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesPatch;
            /**
             * ServerNames is a list of allowed TLS SNI values. If not empty, then
             * TLS must be present and one of the provided SNIs must be indicated in the
             * TLS handshake.
             */
            serverNames: string[];
            terminatingTLS: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSPatch;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsPorts {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * PortProtocol specifies an L4 port with an optional transport protocol
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsPortsPatch {
            /**
             * EndPort can only be an L4 port number.
             */
            endPort: number;
            /**
             * Port can be an L4 port number, or a name in the form of "http"
             * or "http-8080".
             */
            port: string;
            /**
             * Protocol is the L4 protocol. If omitted or empty, any protocol
             * matches. Accepted values: "TCP", "UDP", "SCTP", "ANY"
             *
             * Matching on ICMP is not supported.
             *
             * Named port specified for a container may narrow this down, but may not
             * contradict this.
             */
            protocol: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRules {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesDns[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttp[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesKafka[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesDns {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleDNS is a list of allowed DNS lookups.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesDnsPatch {
            /**
             * MatchName matches literal DNS names. A trailing "." is automatically added
             * when missing.
             */
            matchName: string;
            /**
             * MatchPattern allows using wildcards to match DNS names. All wildcards are
             * case insensitive. The wildcards are:
             * - "*" matches 0 or more DNS valid characters, and may occur anywhere in
             * the pattern. As a special case a "*" as the leftmost character, without a
             * following "." matches all subdomains as well as the name to the right.
             * A trailing "." is automatically added when missing.
             *
             * Examples:
             * `*.cilium.io` matches subomains of cilium at that level
             *   www.cilium.io and blog.cilium.io match, cilium.io and google.com do not
             * `*cilium.io` matches cilium.io and all subdomains ends with "cilium.io"
             *   except those containing "." separator, subcilium.io and sub-cilium.io match,
             *   www.cilium.io and blog.cilium.io does not
             * sub*.cilium.io matches subdomains of cilium where the subdomain component
             * begins with "sub"
             *   sub.cilium.io and subdomain.cilium.io match, www.cilium.io,
             *   blog.cilium.io, cilium.io and google.com do not
             */
            matchPattern: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttp {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatches[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatches {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecret;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * HeaderMatch extends the HeaderValue for matching requirement of a
         * named header field against an immediate string, a secret value, or
         * a regex.  If none of the optional fields is present, then the
         * header value is not matched, only presence of the header is enough.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesPatch {
            /**
             * Mismatch identifies what to do in case there is no match. The default is
             * to drop the request. Otherwise the overall rule is still considered as
             * matching, but the mismatches are logged in the access log.
             */
            mismatch: string;
            /**
             * Name identifies the header.
             */
            name: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecretPatch;
            /**
             * Value matches the exact value of the header. Can be specified either
             * alone or together with "Secret"; will be used as the header value if the
             * secret can not be found in the latter case.
             */
            value: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret refers to a secret that contains the value to be matched against.
         * The secret must only contain one entry. If the referred secret does not
         * exist, and there is no "Value" specified, the match will fail.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * PortRuleHTTP is a list of HTTP protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule does not have any
         * effect.
         *
         * All fields of this type are extended POSIX regex as defined by IEEE Std
         * 1003.1, (i.e this follows the egrep/unix syntax, not the perl syntax)
         * matched against the path of an incoming request. Currently it can contain
         * characters disallowed from the conventional "path" part of a URL as defined
         * by RFC 3986.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesHttpPatch {
            /**
             * HeaderMatches is a list of HTTP headers which must be
             * present and match against the given values. Mismatch field can be used
             * to specify what to do when there is no match.
             */
            headerMatches: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttpHeaderMatchesPatch[];
            /**
             * Headers is a list of HTTP headers which must be present in the
             * request. If omitted or empty, requests are allowed regardless of
             * headers present.
             */
            headers: string[];
            /**
             * Host is an extended POSIX regex matched against the host header of a
             * request. Examples:
             *
             * - foo.bar.com will match the host fooXbar.com or foo-bar.com
             * - foo\.bar\.com will only match the host foo.bar.com
             *
             * If omitted or empty, the value of the host header is ignored.
             */
            host: string;
            /**
             * Method is an extended POSIX regex matched against the method of a
             * request, e.g. "GET", "POST", "PUT", "PATCH", "DELETE", ...
             *
             * If omitted or empty, all methods are allowed.
             */
            method: string;
            /**
             * Path is an extended POSIX regex matched against the path of a
             * request. Currently it can contain characters disallowed from the
             * conventional "path" part of a URL as defined by RFC 3986.
             *
             * If omitted or empty, all paths are all allowed.
             */
            path: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesKafka {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * PortRule is a list of Kafka protocol constraints. All fields are
         * optional, if all fields are empty or missing, the rule will match all
         * Kafka messages.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesKafkaPatch {
            /**
             * APIKey is a case-insensitive string matched against the key of a
             * request, e.g. "produce", "fetch", "createtopic", "deletetopic", et al
             * Reference: https://kafka.apache.org/protocol#protocol_api_keys
             *
             * If omitted or empty, and if Role is not specified, then all keys are allowed.
             */
            apiKey: string;
            /**
             * APIVersion is the version matched against the api version of the
             * Kafka message. If set, it has to be a string representing a positive
             * integer.
             *
             * If omitted or empty, all versions are allowed.
             */
            apiVersion: string;
            /**
             * ClientID is the client identifier as provided in the request.
             *
             * From Kafka protocol documentation:
             * This is a user supplied identifier for the client application. The
             * user can use any identifier they like and it will be used when
             * logging errors, monitoring aggregates, etc. For example, one might
             * want to monitor not just the requests per second overall, but the
             * number coming from each client application (each of which could
             * reside on multiple servers). This id acts as a logical grouping
             * across all requests from a particular client.
             *
             * If omitted or empty, all client identifiers are allowed.
             */
            clientID: string;
            /**
             * Role is a case-insensitive string and describes a group of API keys
             * necessary to perform certain higher-level Kafka operations such as "produce"
             * or "consume". A Role automatically expands into all APIKeys required
             * to perform the specified higher-level operation.
             *
             * The following values are supported:
             *  - "produce": Allow producing to the topics specified in the rule
             *  - "consume": Allow consuming from the topics specified in the rule
             *
             * This field is incompatible with the APIKey field, i.e APIKey and Role
             * cannot both be specified in the same rule.
             *
             * If omitted or empty, and if APIKey is not specified, then all keys are
             * allowed.
             */
            role: string;
            /**
             * Topic is the topic name contained in the message. If a Kafka request
             * contains multiple topics, then all topics must be allowed or the
             * message will be rejected.
             *
             * This constraint is ignored if the matched request message type
             * doesn't contain any topic. Maximum size of Topic can be 249
             * characters as per recent Kafka spec and allowed characters are
             * a-z, A-Z, 0-9, -, . and _.
             *
             * Older Kafka versions had longer topic lengths of 255, but in Kafka 0.10
             * version the length was changed from 255 to 249. For compatibility
             * reasons we are using 255.
             *
             * If omitted or empty, all topics are allowed.
             */
            topic: string;
        }

        /**
         * Rules is a list of additional port level rules which must be met in
         * order for the PortRule to allow the traffic. If omitted or empty,
         * no layer 7 rules are enforced.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsRulesPatch {
            /**
             * DNS-specific rules.
             */
            dns: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesDnsPatch[];
            /**
             * HTTP specific rules.
             */
            http: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesHttpPatch[];
            /**
             * Kafka-specific rules.
             */
            kafka: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsRulesKafkaPatch[];
            /**
             * Key-value pair rules.
             */
            l7: {[key: string]: string}[];
            /**
             * Name of the L7 protocol for which the Key-value pair rules apply.
             */
            l7proto: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsTerminatingTLS {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSSecret;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * TerminatingTLS is the TLS context for the connection terminated by
         * the L7 proxy.  For egress policy this specifies the server-side TLS
         * parameters to be applied on the connections originated from the local
         * endpoint and terminated by the L7 proxy. For ingress policy this specifies
         * the server-side TLS parameters to be applied on the connections
         * originated from a remote source and terminated by the L7 proxy.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSPatch {
            /**
             * Certificate is the file name or k8s secret item name for the certificate
             * chain. If omitted, 'tls.crt' is assumed, if it exists. If given, the
             * item must exist.
             */
            certificate: string;
            /**
             * PrivateKey is the file name or k8s secret item name for the private key
             * matching the certificate chain. If omitted, 'tls.key' is assumed, if it
             * exists. If given, the item must exist.
             */
            privateKey: string;
            secret: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSSecretPatch;
            /**
             * TrustedCA is the file name or k8s secret item name for the trusted CA.
             * If omitted, 'ca.crt' is assumed, if it exists. If given, the item must
             * exist.
             */
            trustedCA: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSSecret {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Secret is the secret that contains the certificates and private key for
         * the TLS context.
         * By default, Cilium will search in this secret for the following items:
         *  - 'ca.crt'  - Which represents the trusted CA to verify remote source.
         *  - 'tls.crt' - Which represents the public key certificate.
         *  - 'tls.key' - Which represents the private key matching the public key
         *                certificate.
         */
        export interface CiliumNetworkPolicySpecsIngressToPortsTerminatingTLSSecretPatch {
            /**
             * Name is the name of the secret.
             */
            name: string;
            /**
             * Namespace is the namespace in which the secret exists. Context of use
             * determines the default value if left out (e.g., "default").
             */
            namespace: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumNetworkPolicySpecsLabels {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * Label is the Cilium's representation of a container label.
         */
        export interface CiliumNetworkPolicySpecsLabelsPatch {
            key: string;
            /**
             * Source can be one of the above values (e.g.: LabelSourceContainer).
             */
            source: string;
            value: string;
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumNetworkPolicySpecsNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNetworkPolicySpecsNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects all nodes which should be subject to this rule.
         * EndpointSelector and NodeSelector cannot be both empty and are mutually
         * exclusive. Can only be used in CiliumClusterwideNetworkPolicies.
         */
        export interface CiliumNetworkPolicySpecsNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNetworkPolicySpecsNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Rule is a policy rule which must be applied to all endpoints which match the
         * labels contained in the endpointSelector
         *
         * Each rule is split into an ingress section which contains all rules
         * applicable at ingress, and an egress section applicable at egress. For rule
         * types such as `L4Rule` and `CIDR` which can be applied at both ingress and
         * egress, both ingress and egress side have to either specifically allow the
         * connection or one side has to be omitted.
         *
         * Either ingress, egress, or both can be provided. If both ingress and egress
         * are omitted, the rule has no effect.
         */
        export interface CiliumNetworkPolicySpecsPatch {
            /**
             * Description is a free form string, it can be used by the creator of
             * the rule to store human readable explanation of the purpose of this
             * rule. Rules cannot be identified by comment.
             */
            description: string;
            /**
             * Egress is a list of EgressRule which are enforced at egress.
             * If omitted or empty, this rule does not apply at egress.
             */
            egress: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressPatch[];
            /**
             * EgressDeny is a list of EgressDenyRule which are enforced at egress.
             * Any rule inserted here will be denied regardless of the allowed egress
             * rules in the 'egress' field.
             * If omitted or empty, this rule does not apply at egress.
             */
            egressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsEgressDenyPatch[];
            enableDefaultDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsEnableDefaultDenyPatch;
            endpointSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsEndpointSelectorPatch;
            /**
             * Ingress is a list of IngressRule which are enforced at ingress.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingress: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressPatch[];
            /**
             * IngressDeny is a list of IngressDenyRule which are enforced at ingress.
             * Any rule inserted here will be denied regardless of the allowed ingress
             * rules in the 'ingress' field.
             * If omitted or empty, this rule does not apply at ingress.
             */
            ingressDeny: outputs.cilium.v2.CiliumNetworkPolicySpecsIngressDenyPatch[];
            /**
             * Labels is a list of optional strings which can be used to
             * re-identify the rule or to store metadata. It is possible to lookup
             * or delete strings based on labels. Labels are not required to be
             * unique, multiple rules can have overlapping or identical labels.
             */
            labels: outputs.cilium.v2.CiliumNetworkPolicySpecsLabelsPatch[];
            nodeSelector: outputs.cilium.v2.CiliumNetworkPolicySpecsNodeSelectorPatch;
        }

        /**
         * Status is the status of the Cilium policy rule
         */
        export interface CiliumNetworkPolicyStatus {
            conditions: outputs.cilium.v2.CiliumNetworkPolicyStatusConditions[];
            /**
             * DerivativePolicies is the status of all policies derived from the Cilium
             * policy
             */
            derivativePolicies: {[key: string]: {[key: string]: string}};
        }

        export interface CiliumNetworkPolicyStatusConditions {
            /**
             * The last time the condition transitioned from one status to another.
             */
            lastTransitionTime: string;
            /**
             * A human readable message indicating details about the transition.
             */
            message: string;
            /**
             * The reason for the condition's last transition.
             */
            reason: string;
            /**
             * The status of the condition, one of True, False, or Unknown
             */
            status: string;
            /**
             * The type of the policy condition
             */
            type: string;
        }

        export interface CiliumNetworkPolicyStatusConditionsPatch {
            /**
             * The last time the condition transitioned from one status to another.
             */
            lastTransitionTime: string;
            /**
             * A human readable message indicating details about the transition.
             */
            message: string;
            /**
             * The reason for the condition's last transition.
             */
            reason: string;
            /**
             * The status of the condition, one of True, False, or Unknown
             */
            status: string;
            /**
             * The type of the policy condition
             */
            type: string;
        }

        /**
         * Status is the status of the Cilium policy rule
         */
        export interface CiliumNetworkPolicyStatusPatch {
            conditions: outputs.cilium.v2.CiliumNetworkPolicyStatusConditionsPatch[];
            /**
             * DerivativePolicies is the status of all policies derived from the Cilium
             * policy
             */
            derivativePolicies: {[key: string]: {[key: string]: string}};
        }

        /**
         * CiliumNode represents a node managed by Cilium. It contains a specification
         * to control various node specific configuration aspects and a status section
         * to represent the status of the node.
         */
        export interface CiliumNode {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumNode";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumNodeSpec;
            status: outputs.cilium.v2.CiliumNodeStatus;
        }

        /**
         * CiliumNodeConfig is a list of configuration key-value pairs. It is applied to
         * nodes indicated by a label selector.
         *
         * If multiple overrides apply to the same node, they will be ordered by name
         * with later Overrides overwriting any conflicting keys.
         */
        export interface CiliumNodeConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumNodeConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2.CiliumNodeConfigSpec;
        }

        /**
         * Spec is the desired Cilium configuration overrides for a given node
         */
        export interface CiliumNodeConfigSpec {
            /**
             * Defaults is treated the same as the cilium-config ConfigMap - a set
             * of key-value pairs parsed by the agent and operator processes.
             * Each key must be a valid config-map data field (i.e. a-z, A-Z, -, _, and .)
             */
            defaults: {[key: string]: string};
            nodeSelector: outputs.cilium.v2.CiliumNodeConfigSpecNodeSelector;
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If not supplied, then this config applies to no nodes. If
         * empty, then it applies to all nodes.
         */
        export interface CiliumNodeConfigSpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNodeConfigSpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNodeConfigSpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNodeConfigSpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If not supplied, then this config applies to no nodes. If
         * empty, then it applies to all nodes.
         */
        export interface CiliumNodeConfigSpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2.CiliumNodeConfigSpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is the desired Cilium configuration overrides for a given node
         */
        export interface CiliumNodeConfigSpecPatch {
            /**
             * Defaults is treated the same as the cilium-config ConfigMap - a set
             * of key-value pairs parsed by the agent and operator processes.
             * Each key must be a valid config-map data field (i.e. a-z, A-Z, -, _, and .)
             */
            defaults: {[key: string]: string};
            nodeSelector: outputs.cilium.v2.CiliumNodeConfigSpecNodeSelectorPatch;
        }

        /**
         * Spec defines the desired specification/configuration of the node.
         */
        export interface CiliumNodeSpec {
            /**
             * Addresses is the list of all node addresses.
             */
            addresses: outputs.cilium.v2.CiliumNodeSpecAddresses[];
            "alibaba-cloud": outputs.cilium.v2.CiliumNodeSpecAlibabaCloud;
            azure: outputs.cilium.v2.CiliumNodeSpecAzure;
            /**
             * BootID is a unique node identifier generated on boot
             */
            bootid: string;
            encryption: outputs.cilium.v2.CiliumNodeSpecEncryption;
            eni: outputs.cilium.v2.CiliumNodeSpecEni;
            health: outputs.cilium.v2.CiliumNodeSpecHealth;
            ingress: outputs.cilium.v2.CiliumNodeSpecIngress;
            /**
             * InstanceID is the identifier of the node. This is different from the
             * node name which is typically the FQDN of the node. The InstanceID
             * typically refers to the identifier used by the cloud provider or
             * some other means of identification.
             */
            "instance-id": string;
            ipam: outputs.cilium.v2.CiliumNodeSpecIpam;
            /**
             * NodeIdentity is the Cilium numeric identity allocated for the node, if any.
             */
            nodeidentity: number;
        }

        /**
         * NodeAddress is a node address.
         */
        export interface CiliumNodeSpecAddresses {
            /**
             * IP is an IP of a node
             */
            ip: string;
            /**
             * Type is the type of the node address
             */
            type: string;
        }

        /**
         * NodeAddress is a node address.
         */
        export interface CiliumNodeSpecAddressesPatch {
            /**
             * IP is an IP of a node
             */
            ip: string;
            /**
             * Type is the type of the node address
             */
            type: string;
        }

        /**
         * AlibabaCloud is the AlibabaCloud IPAM specific configuration.
         */
        export interface CiliumNodeSpecAlibabaCloud {
            /**
             * AvailabilityZone is the availability zone to use when allocating
             * ENIs.
             */
            "availability-zone": string;
            /**
             * CIDRBlock is vpc ipv4 CIDR
             */
            "cidr-block": string;
            /**
             * InstanceType is the ECS instance type, e.g. "ecs.g6.2xlarge"
             */
            "instance-type": string;
            /**
             * SecurityGroupTags is the list of tags to use when evaluating which
             * security groups to use for the ENI.
             */
            "security-group-tags": {[key: string]: string};
            /**
             * SecurityGroups is the list of security groups to attach to any ENI
             * that is created and attached to the instance.
             */
            "security-groups": string[];
            /**
             * VPCID is the VPC ID to use when allocating ENIs.
             */
            "vpc-id": string;
            /**
             * VSwitchTags is the list of tags to use when evaluating which
             * vSwitch to use for the ENI.
             */
            "vswitch-tags": {[key: string]: string};
            /**
             * VSwitches is the ID of vSwitch available for ENI
             */
            vswitches: string[];
        }

        /**
         * AlibabaCloud is the AlibabaCloud IPAM specific configuration.
         */
        export interface CiliumNodeSpecAlibabaCloudPatch {
            /**
             * AvailabilityZone is the availability zone to use when allocating
             * ENIs.
             */
            "availability-zone": string;
            /**
             * CIDRBlock is vpc ipv4 CIDR
             */
            "cidr-block": string;
            /**
             * InstanceType is the ECS instance type, e.g. "ecs.g6.2xlarge"
             */
            "instance-type": string;
            /**
             * SecurityGroupTags is the list of tags to use when evaluating which
             * security groups to use for the ENI.
             */
            "security-group-tags": {[key: string]: string};
            /**
             * SecurityGroups is the list of security groups to attach to any ENI
             * that is created and attached to the instance.
             */
            "security-groups": string[];
            /**
             * VPCID is the VPC ID to use when allocating ENIs.
             */
            "vpc-id": string;
            /**
             * VSwitchTags is the list of tags to use when evaluating which
             * vSwitch to use for the ENI.
             */
            "vswitch-tags": {[key: string]: string};
            /**
             * VSwitches is the ID of vSwitch available for ENI
             */
            vswitches: string[];
        }

        /**
         * Azure is the Azure IPAM specific configuration.
         */
        export interface CiliumNodeSpecAzure {
            /**
             * InterfaceName is the name of the interface the cilium-operator
             * will use to allocate all the IPs on
             */
            "interface-name": string;
        }

        /**
         * Azure is the Azure IPAM specific configuration.
         */
        export interface CiliumNodeSpecAzurePatch {
            /**
             * InterfaceName is the name of the interface the cilium-operator
             * will use to allocate all the IPs on
             */
            "interface-name": string;
        }

        /**
         * Encryption is the encryption configuration of the node.
         */
        export interface CiliumNodeSpecEncryption {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * Encryption is the encryption configuration of the node.
         */
        export interface CiliumNodeSpecEncryptionPatch {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * ENI is the AWS ENI specific configuration.
         */
        export interface CiliumNodeSpecEni {
            /**
             * AvailabilityZone is the availability zone to use when allocating
             * ENIs.
             */
            "availability-zone": string;
            /**
             * DeleteOnTermination defines that the ENI should be deleted when the
             * associated instance is terminated. If the parameter is not set the
             * default behavior is to delete the ENI on instance termination.
             */
            "delete-on-termination": boolean;
            /**
             * DisablePrefixDelegation determines whether ENI prefix delegation should be
             * disabled on this node.
             */
            "disable-prefix-delegation": boolean;
            /**
             * ExcludeInterfaceTags is the list of tags to use when excluding ENIs for
             * Cilium IP allocation. Any interface matching this set of tags will not
             * be managed by Cilium.
             */
            "exclude-interface-tags": {[key: string]: string};
            /**
             * FirstInterfaceIndex is the index of the first ENI to use for IP
             * allocation, e.g. if the node has eth0, eth1, eth2 and
             * FirstInterfaceIndex is set to 1, then only eth1 and eth2 will be
             * used for IP allocation, eth0 will be ignored for PodIP allocation.
             */
            "first-interface-index": number;
            /**
             * InstanceID is the AWS InstanceId of the node. The InstanceID is used
             * to retrieve AWS metadata for the node.
             *
             * OBSOLETE: This field is obsolete, please use Spec.InstanceID
             */
            "instance-id": string;
            /**
             * InstanceType is the AWS EC2 instance type, e.g. "m5.large"
             */
            "instance-type": string;
            /**
             * MaxAboveWatermark is the maximum number of addresses to allocate
             * beyond the addresses needed to reach the PreAllocate watermark.
             * Going above the watermark can help reduce the number of API calls to
             * allocate IPs, e.g. when a new ENI is allocated, as many secondary
             * IPs as possible are allocated. Limiting the amount can help reduce
             * waste of IPs.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.MaxAboveWatermark
             */
            "max-above-watermark": number;
            /**
             * MinAllocate is the minimum number of IPs that must be allocated when
             * the node is first bootstrapped. It defines the minimum base socket
             * of addresses that must be available. After reaching this watermark,
             * the PreAllocate and MaxAboveWatermark logic takes over to continue
             * allocating IPs.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.MinAllocate
             */
            "min-allocate": number;
            /**
             * NodeSubnetID is the subnet of the primary ENI the instance was brought up
             * with. It is used as a sensible default subnet to create ENIs in.
             */
            "node-subnet-id": string;
            /**
             * PreAllocate defines the number of IP addresses that must be
             * available for allocation in the IPAMspec. It defines the buffer of
             * addresses available immediately without requiring cilium-operator to
             * get involved.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.PreAllocate
             */
            "pre-allocate": number;
            /**
             * SecurityGroupTags is the list of tags to use when evaliating what
             * AWS security groups to use for the ENI.
             */
            "security-group-tags": {[key: string]: string};
            /**
             * SecurityGroups is the list of security groups to attach to any ENI
             * that is created and attached to the instance.
             */
            "security-groups": string[];
            /**
             * SubnetIDs is the list of subnet ids to use when evaluating what AWS
             * subnets to use for ENI and IP allocation.
             */
            "subnet-ids": string[];
            /**
             * SubnetTags is the list of tags to use when evaluating what AWS
             * subnets to use for ENI and IP allocation.
             */
            "subnet-tags": {[key: string]: string};
            /**
             * UsePrimaryAddress determines whether an ENI's primary address
             * should be available for allocations on the node
             */
            "use-primary-address": boolean;
            /**
             * VpcID is the VPC ID to use when allocating ENIs.
             */
            "vpc-id": string;
        }

        /**
         * ENI is the AWS ENI specific configuration.
         */
        export interface CiliumNodeSpecEniPatch {
            /**
             * AvailabilityZone is the availability zone to use when allocating
             * ENIs.
             */
            "availability-zone": string;
            /**
             * DeleteOnTermination defines that the ENI should be deleted when the
             * associated instance is terminated. If the parameter is not set the
             * default behavior is to delete the ENI on instance termination.
             */
            "delete-on-termination": boolean;
            /**
             * DisablePrefixDelegation determines whether ENI prefix delegation should be
             * disabled on this node.
             */
            "disable-prefix-delegation": boolean;
            /**
             * ExcludeInterfaceTags is the list of tags to use when excluding ENIs for
             * Cilium IP allocation. Any interface matching this set of tags will not
             * be managed by Cilium.
             */
            "exclude-interface-tags": {[key: string]: string};
            /**
             * FirstInterfaceIndex is the index of the first ENI to use for IP
             * allocation, e.g. if the node has eth0, eth1, eth2 and
             * FirstInterfaceIndex is set to 1, then only eth1 and eth2 will be
             * used for IP allocation, eth0 will be ignored for PodIP allocation.
             */
            "first-interface-index": number;
            /**
             * InstanceID is the AWS InstanceId of the node. The InstanceID is used
             * to retrieve AWS metadata for the node.
             *
             * OBSOLETE: This field is obsolete, please use Spec.InstanceID
             */
            "instance-id": string;
            /**
             * InstanceType is the AWS EC2 instance type, e.g. "m5.large"
             */
            "instance-type": string;
            /**
             * MaxAboveWatermark is the maximum number of addresses to allocate
             * beyond the addresses needed to reach the PreAllocate watermark.
             * Going above the watermark can help reduce the number of API calls to
             * allocate IPs, e.g. when a new ENI is allocated, as many secondary
             * IPs as possible are allocated. Limiting the amount can help reduce
             * waste of IPs.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.MaxAboveWatermark
             */
            "max-above-watermark": number;
            /**
             * MinAllocate is the minimum number of IPs that must be allocated when
             * the node is first bootstrapped. It defines the minimum base socket
             * of addresses that must be available. After reaching this watermark,
             * the PreAllocate and MaxAboveWatermark logic takes over to continue
             * allocating IPs.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.MinAllocate
             */
            "min-allocate": number;
            /**
             * NodeSubnetID is the subnet of the primary ENI the instance was brought up
             * with. It is used as a sensible default subnet to create ENIs in.
             */
            "node-subnet-id": string;
            /**
             * PreAllocate defines the number of IP addresses that must be
             * available for allocation in the IPAMspec. It defines the buffer of
             * addresses available immediately without requiring cilium-operator to
             * get involved.
             *
             * OBSOLETE: This field is obsolete, please use Spec.IPAM.PreAllocate
             */
            "pre-allocate": number;
            /**
             * SecurityGroupTags is the list of tags to use when evaliating what
             * AWS security groups to use for the ENI.
             */
            "security-group-tags": {[key: string]: string};
            /**
             * SecurityGroups is the list of security groups to attach to any ENI
             * that is created and attached to the instance.
             */
            "security-groups": string[];
            /**
             * SubnetIDs is the list of subnet ids to use when evaluating what AWS
             * subnets to use for ENI and IP allocation.
             */
            "subnet-ids": string[];
            /**
             * SubnetTags is the list of tags to use when evaluating what AWS
             * subnets to use for ENI and IP allocation.
             */
            "subnet-tags": {[key: string]: string};
            /**
             * UsePrimaryAddress determines whether an ENI's primary address
             * should be available for allocations on the node
             */
            "use-primary-address": boolean;
            /**
             * VpcID is the VPC ID to use when allocating ENIs.
             */
            "vpc-id": string;
        }

        /**
         * HealthAddressing is the addressing information for health connectivity
         * checking.
         */
        export interface CiliumNodeSpecHealth {
            /**
             * IPv4 is the IPv4 address of the IPv4 health endpoint.
             */
            ipv4: string;
            /**
             * IPv6 is the IPv6 address of the IPv4 health endpoint.
             */
            ipv6: string;
        }

        /**
         * HealthAddressing is the addressing information for health connectivity
         * checking.
         */
        export interface CiliumNodeSpecHealthPatch {
            /**
             * IPv4 is the IPv4 address of the IPv4 health endpoint.
             */
            ipv4: string;
            /**
             * IPv6 is the IPv6 address of the IPv4 health endpoint.
             */
            ipv6: string;
        }

        /**
         * IngressAddressing is the addressing information for Ingress listener.
         */
        export interface CiliumNodeSpecIngress {
            ipv4: string;
            ipv6: string;
        }

        /**
         * IngressAddressing is the addressing information for Ingress listener.
         */
        export interface CiliumNodeSpecIngressPatch {
            ipv4: string;
            ipv6: string;
        }

        /**
         * IPAM is the address management specification. This section can be
         * populated by a user or it can be automatically populated by an IPAM
         * operator.
         */
        export interface CiliumNodeSpecIpam {
            /**
             * IPv6Pool is the list of IPv6 addresses available to the node for allocation.
             * When an IPv6 address is used, it will remain on this list but will be added to
             * Status.IPAM.IPv6Used
             */
            "ipv6-pool": {[key: string]: {[key: string]: string}};
            /**
             * MaxAboveWatermark is the maximum number of addresses to allocate
             * beyond the addresses needed to reach the PreAllocate watermark.
             * Going above the watermark can help reduce the number of API calls to
             * allocate IPs, e.g. when a new ENI is allocated, as many secondary
             * IPs as possible are allocated. Limiting the amount can help reduce
             * waste of IPs.
             */
            "max-above-watermark": number;
            /**
             * MaxAllocate is the maximum number of IPs that can be allocated to the
             * node. When the current amount of allocated IPs will approach this value,
             * the considered value for PreAllocate will decrease down to 0 in order to
             * not attempt to allocate more addresses than defined.
             */
            "max-allocate": number;
            /**
             * MinAllocate is the minimum number of IPs that must be allocated when
             * the node is first bootstrapped. It defines the minimum base socket
             * of addresses that must be available. After reaching this watermark,
             * the PreAllocate and MaxAboveWatermark logic takes over to continue
             * allocating IPs.
             */
            "min-allocate": number;
            /**
             * PodCIDRs is the list of CIDRs available to the node for allocation.
             * When an IP is used, the IP will be added to Status.IPAM.Used
             */
            podCIDRs: string[];
            /**
             * Pool is the list of IPv4 addresses available to the node for allocation.
             * When an IPv4 address is used, it will remain on this list but will be added to
             * Status.IPAM.Used
             */
            pool: {[key: string]: {[key: string]: string}};
            pools: outputs.cilium.v2.CiliumNodeSpecIpamPools;
            /**
             * PreAllocate defines the number of IP addresses that must be
             * available for allocation in the IPAMspec. It defines the buffer of
             * addresses available immediately without requiring cilium-operator to
             * get involved.
             */
            "pre-allocate": number;
            /**
             * StaticIPTags are used to determine the pool of IPs from which to
             * attribute a static IP to the node. For example in AWS this is used to
             * filter Elastic IP Addresses.
             */
            "static-ip-tags": {[key: string]: string};
        }

        /**
         * IPAM is the address management specification. This section can be
         * populated by a user or it can be automatically populated by an IPAM
         * operator.
         */
        export interface CiliumNodeSpecIpamPatch {
            /**
             * IPv6Pool is the list of IPv6 addresses available to the node for allocation.
             * When an IPv6 address is used, it will remain on this list but will be added to
             * Status.IPAM.IPv6Used
             */
            "ipv6-pool": {[key: string]: {[key: string]: string}};
            /**
             * MaxAboveWatermark is the maximum number of addresses to allocate
             * beyond the addresses needed to reach the PreAllocate watermark.
             * Going above the watermark can help reduce the number of API calls to
             * allocate IPs, e.g. when a new ENI is allocated, as many secondary
             * IPs as possible are allocated. Limiting the amount can help reduce
             * waste of IPs.
             */
            "max-above-watermark": number;
            /**
             * MaxAllocate is the maximum number of IPs that can be allocated to the
             * node. When the current amount of allocated IPs will approach this value,
             * the considered value for PreAllocate will decrease down to 0 in order to
             * not attempt to allocate more addresses than defined.
             */
            "max-allocate": number;
            /**
             * MinAllocate is the minimum number of IPs that must be allocated when
             * the node is first bootstrapped. It defines the minimum base socket
             * of addresses that must be available. After reaching this watermark,
             * the PreAllocate and MaxAboveWatermark logic takes over to continue
             * allocating IPs.
             */
            "min-allocate": number;
            /**
             * PodCIDRs is the list of CIDRs available to the node for allocation.
             * When an IP is used, the IP will be added to Status.IPAM.Used
             */
            podCIDRs: string[];
            /**
             * Pool is the list of IPv4 addresses available to the node for allocation.
             * When an IPv4 address is used, it will remain on this list but will be added to
             * Status.IPAM.Used
             */
            pool: {[key: string]: {[key: string]: string}};
            pools: outputs.cilium.v2.CiliumNodeSpecIpamPoolsPatch;
            /**
             * PreAllocate defines the number of IP addresses that must be
             * available for allocation in the IPAMspec. It defines the buffer of
             * addresses available immediately without requiring cilium-operator to
             * get involved.
             */
            "pre-allocate": number;
            /**
             * StaticIPTags are used to determine the pool of IPs from which to
             * attribute a static IP to the node. For example in AWS this is used to
             * filter Elastic IP Addresses.
             */
            "static-ip-tags": {[key: string]: string};
        }

        /**
         * Pools contains the list of assigned IPAM pools for this node.
         */
        export interface CiliumNodeSpecIpamPools {
            /**
             * Allocated contains the list of pooled CIDR assigned to this node. The
             * operator will add new pod CIDRs to this field, whereas the agent will
             * remove CIDRs it has released.
             */
            allocated: outputs.cilium.v2.CiliumNodeSpecIpamPoolsAllocated[];
            /**
             * Requested contains a list of IPAM pool requests, i.e. indicates how many
             * addresses this node requests out of each pool listed here. This field
             * is owned and written to by cilium-agent and read by the operator.
             */
            requested: outputs.cilium.v2.CiliumNodeSpecIpamPoolsRequested[];
        }

        /**
         * IPAMPoolAllocation describes an allocation of an IPAM pool from the operator to the
         * node. It contains the assigned PodCIDRs allocated from this pool
         */
        export interface CiliumNodeSpecIpamPoolsAllocated {
            /**
             * CIDRs contains a list of pod CIDRs currently allocated from this pool
             */
            cidrs: string[];
            /**
             * Pool is the name of the IPAM pool backing this allocation
             */
            pool: string;
        }

        /**
         * IPAMPoolAllocation describes an allocation of an IPAM pool from the operator to the
         * node. It contains the assigned PodCIDRs allocated from this pool
         */
        export interface CiliumNodeSpecIpamPoolsAllocatedPatch {
            /**
             * CIDRs contains a list of pod CIDRs currently allocated from this pool
             */
            cidrs: string[];
            /**
             * Pool is the name of the IPAM pool backing this allocation
             */
            pool: string;
        }

        /**
         * Pools contains the list of assigned IPAM pools for this node.
         */
        export interface CiliumNodeSpecIpamPoolsPatch {
            /**
             * Allocated contains the list of pooled CIDR assigned to this node. The
             * operator will add new pod CIDRs to this field, whereas the agent will
             * remove CIDRs it has released.
             */
            allocated: outputs.cilium.v2.CiliumNodeSpecIpamPoolsAllocatedPatch[];
            /**
             * Requested contains a list of IPAM pool requests, i.e. indicates how many
             * addresses this node requests out of each pool listed here. This field
             * is owned and written to by cilium-agent and read by the operator.
             */
            requested: outputs.cilium.v2.CiliumNodeSpecIpamPoolsRequestedPatch[];
        }

        export interface CiliumNodeSpecIpamPoolsRequested {
            needed: outputs.cilium.v2.CiliumNodeSpecIpamPoolsRequestedNeeded;
            /**
             * Pool is the name of the IPAM pool backing this request
             */
            pool: string;
        }

        /**
         * Needed indicates how many IPs out of the above Pool this node requests
         * from the operator. The operator runs a reconciliation loop to ensure each
         * node always has enough PodCIDRs allocated in each pool to fulfill the
         * requested number of IPs here.
         */
        export interface CiliumNodeSpecIpamPoolsRequestedNeeded {
            /**
             * IPv4Addrs contains the number of requested IPv4 addresses out of a given
             * pool
             */
            "ipv4-addrs": number;
            /**
             * IPv6Addrs contains the number of requested IPv6 addresses out of a given
             * pool
             */
            "ipv6-addrs": number;
        }

        /**
         * Needed indicates how many IPs out of the above Pool this node requests
         * from the operator. The operator runs a reconciliation loop to ensure each
         * node always has enough PodCIDRs allocated in each pool to fulfill the
         * requested number of IPs here.
         */
        export interface CiliumNodeSpecIpamPoolsRequestedNeededPatch {
            /**
             * IPv4Addrs contains the number of requested IPv4 addresses out of a given
             * pool
             */
            "ipv4-addrs": number;
            /**
             * IPv6Addrs contains the number of requested IPv6 addresses out of a given
             * pool
             */
            "ipv6-addrs": number;
        }

        export interface CiliumNodeSpecIpamPoolsRequestedPatch {
            needed: outputs.cilium.v2.CiliumNodeSpecIpamPoolsRequestedNeededPatch;
            /**
             * Pool is the name of the IPAM pool backing this request
             */
            pool: string;
        }

        /**
         * Spec defines the desired specification/configuration of the node.
         */
        export interface CiliumNodeSpecPatch {
            /**
             * Addresses is the list of all node addresses.
             */
            addresses: outputs.cilium.v2.CiliumNodeSpecAddressesPatch[];
            "alibaba-cloud": outputs.cilium.v2.CiliumNodeSpecAlibabaCloudPatch;
            azure: outputs.cilium.v2.CiliumNodeSpecAzurePatch;
            /**
             * BootID is a unique node identifier generated on boot
             */
            bootid: string;
            encryption: outputs.cilium.v2.CiliumNodeSpecEncryptionPatch;
            eni: outputs.cilium.v2.CiliumNodeSpecEniPatch;
            health: outputs.cilium.v2.CiliumNodeSpecHealthPatch;
            ingress: outputs.cilium.v2.CiliumNodeSpecIngressPatch;
            /**
             * InstanceID is the identifier of the node. This is different from the
             * node name which is typically the FQDN of the node. The InstanceID
             * typically refers to the identifier used by the cloud provider or
             * some other means of identification.
             */
            "instance-id": string;
            ipam: outputs.cilium.v2.CiliumNodeSpecIpamPatch;
            /**
             * NodeIdentity is the Cilium numeric identity allocated for the node, if any.
             */
            nodeidentity: number;
        }

        /**
         * Status defines the realized specification/configuration and status
         * of the node.
         */
        export interface CiliumNodeStatus {
            "alibaba-cloud": outputs.cilium.v2.CiliumNodeStatusAlibabaCloud;
            azure: outputs.cilium.v2.CiliumNodeStatusAzure;
            eni: outputs.cilium.v2.CiliumNodeStatusEni;
            ipam: outputs.cilium.v2.CiliumNodeStatusIpam;
        }

        /**
         * AlibabaCloud is the AlibabaCloud specific status of the node.
         */
        export interface CiliumNodeStatusAlibabaCloud {
            /**
             * ENIs is the list of ENIs on the node
             */
            enis: {[key: string]: {[key: string]: string}};
        }

        /**
         * AlibabaCloud is the AlibabaCloud specific status of the node.
         */
        export interface CiliumNodeStatusAlibabaCloudPatch {
            /**
             * ENIs is the list of ENIs on the node
             */
            enis: {[key: string]: {[key: string]: string}};
        }

        /**
         * Azure is the Azure specific status of the node.
         */
        export interface CiliumNodeStatusAzure {
            /**
             * Interfaces is the list of interfaces on the node
             */
            interfaces: outputs.cilium.v2.CiliumNodeStatusAzureInterfaces[];
        }

        /**
         * AzureInterface represents an Azure Interface
         */
        export interface CiliumNodeStatusAzureInterfaces {
            /**
             * GatewayIP is the interface's subnet's default route
             *
             * OBSOLETE: This field is obsolete, please use Gateway field instead.
             */
            GatewayIP: string;
            /**
             * Addresses is the list of all IPs associated with the interface,
             * including all secondary addresses
             */
            addresses: outputs.cilium.v2.CiliumNodeStatusAzureInterfacesAddresses[];
            /**
             * CIDR is the range that the interface belongs to.
             */
            cidr: string;
            /**
             * Gateway is the interface's subnet's default route
             */
            gateway: string;
            /**
             * ID is the identifier
             */
            id: string;
            /**
             * MAC is the mac address
             */
            mac: string;
            /**
             * Name is the name of the interface
             */
            name: string;
            /**
             * SecurityGroup is the security group associated with the interface
             */
            "security-group": string;
            /**
             * State is the provisioning state
             */
            state: string;
        }

        /**
         * AzureAddress is an IP address assigned to an AzureInterface
         */
        export interface CiliumNodeStatusAzureInterfacesAddresses {
            /**
             * IP is the ip address of the address
             */
            ip: string;
            /**
             * State is the provisioning state of the address
             */
            state: string;
            /**
             * Subnet is the subnet the address belongs to
             */
            subnet: string;
        }

        /**
         * AzureAddress is an IP address assigned to an AzureInterface
         */
        export interface CiliumNodeStatusAzureInterfacesAddressesPatch {
            /**
             * IP is the ip address of the address
             */
            ip: string;
            /**
             * State is the provisioning state of the address
             */
            state: string;
            /**
             * Subnet is the subnet the address belongs to
             */
            subnet: string;
        }

        /**
         * AzureInterface represents an Azure Interface
         */
        export interface CiliumNodeStatusAzureInterfacesPatch {
            /**
             * GatewayIP is the interface's subnet's default route
             *
             * OBSOLETE: This field is obsolete, please use Gateway field instead.
             */
            GatewayIP: string;
            /**
             * Addresses is the list of all IPs associated with the interface,
             * including all secondary addresses
             */
            addresses: outputs.cilium.v2.CiliumNodeStatusAzureInterfacesAddressesPatch[];
            /**
             * CIDR is the range that the interface belongs to.
             */
            cidr: string;
            /**
             * Gateway is the interface's subnet's default route
             */
            gateway: string;
            /**
             * ID is the identifier
             */
            id: string;
            /**
             * MAC is the mac address
             */
            mac: string;
            /**
             * Name is the name of the interface
             */
            name: string;
            /**
             * SecurityGroup is the security group associated with the interface
             */
            "security-group": string;
            /**
             * State is the provisioning state
             */
            state: string;
        }

        /**
         * Azure is the Azure specific status of the node.
         */
        export interface CiliumNodeStatusAzurePatch {
            /**
             * Interfaces is the list of interfaces on the node
             */
            interfaces: outputs.cilium.v2.CiliumNodeStatusAzureInterfacesPatch[];
        }

        /**
         * ENI is the AWS ENI specific status of the node.
         */
        export interface CiliumNodeStatusEni {
            /**
             * ENIs is the list of ENIs on the node
             */
            enis: {[key: string]: {[key: string]: string}};
        }

        /**
         * ENI is the AWS ENI specific status of the node.
         */
        export interface CiliumNodeStatusEniPatch {
            /**
             * ENIs is the list of ENIs on the node
             */
            enis: {[key: string]: {[key: string]: string}};
        }

        /**
         * IPAM is the IPAM status of the node.
         */
        export interface CiliumNodeStatusIpam {
            /**
             * AssignedStaticIP is the static IP assigned to the node (ex: public Elastic IP address in AWS)
             */
            "assigned-static-ip": string;
            /**
             * IPv6Used lists all IPv6 addresses out of Spec.IPAM.IPv6Pool which have been
             * allocated and are in use.
             */
            "ipv6-used": {[key: string]: {[key: string]: string}};
            "operator-status": outputs.cilium.v2.CiliumNodeStatusIpamOperatorStatus;
            /**
             * PodCIDRs lists the status of each pod CIDR allocated to this node.
             */
            "pod-cidrs": {[key: string]: {[key: string]: string}};
            /**
             * ReleaseIPs tracks the state for every IPv4 address considered for release.
             * The value can be one of the following strings:
             * * marked-for-release : Set by operator as possible candidate for IP
             * * ready-for-release  : Acknowledged as safe to release by agent
             * * do-not-release     : IP already in use / not owned by the node. Set by agent
             * * released           : IP successfully released. Set by operator
             */
            "release-ips": {[key: string]: string};
            /**
             * ReleaseIPv6s tracks the state for every IPv6 address considered for release.
             * The value can be one of the following strings:
             * * marked-for-release : Set by operator as possible candidate for IP
             * * ready-for-release  : Acknowledged as safe to release by agent
             * * do-not-release     : IP already in use / not owned by the node. Set by agent
             * * released           : IP successfully released. Set by operator
             */
            "release-ipv6s": {[key: string]: string};
            /**
             * Used lists all IPv4 addresses out of Spec.IPAM.Pool which have been allocated
             * and are in use.
             */
            used: {[key: string]: {[key: string]: string}};
        }

        /**
         * Operator is the Operator status of the node
         */
        export interface CiliumNodeStatusIpamOperatorStatus {
            /**
             * Error is the error message set by cilium-operator.
             */
            error: string;
        }

        /**
         * Operator is the Operator status of the node
         */
        export interface CiliumNodeStatusIpamOperatorStatusPatch {
            /**
             * Error is the error message set by cilium-operator.
             */
            error: string;
        }

        /**
         * IPAM is the IPAM status of the node.
         */
        export interface CiliumNodeStatusIpamPatch {
            /**
             * AssignedStaticIP is the static IP assigned to the node (ex: public Elastic IP address in AWS)
             */
            "assigned-static-ip": string;
            /**
             * IPv6Used lists all IPv6 addresses out of Spec.IPAM.IPv6Pool which have been
             * allocated and are in use.
             */
            "ipv6-used": {[key: string]: {[key: string]: string}};
            "operator-status": outputs.cilium.v2.CiliumNodeStatusIpamOperatorStatusPatch;
            /**
             * PodCIDRs lists the status of each pod CIDR allocated to this node.
             */
            "pod-cidrs": {[key: string]: {[key: string]: string}};
            /**
             * ReleaseIPs tracks the state for every IPv4 address considered for release.
             * The value can be one of the following strings:
             * * marked-for-release : Set by operator as possible candidate for IP
             * * ready-for-release  : Acknowledged as safe to release by agent
             * * do-not-release     : IP already in use / not owned by the node. Set by agent
             * * released           : IP successfully released. Set by operator
             */
            "release-ips": {[key: string]: string};
            /**
             * ReleaseIPv6s tracks the state for every IPv6 address considered for release.
             * The value can be one of the following strings:
             * * marked-for-release : Set by operator as possible candidate for IP
             * * ready-for-release  : Acknowledged as safe to release by agent
             * * do-not-release     : IP already in use / not owned by the node. Set by agent
             * * released           : IP successfully released. Set by operator
             */
            "release-ipv6s": {[key: string]: string};
            /**
             * Used lists all IPv4 addresses out of Spec.IPAM.Pool which have been allocated
             * and are in use.
             */
            used: {[key: string]: {[key: string]: string}};
        }

        /**
         * Status defines the realized specification/configuration and status
         * of the node.
         */
        export interface CiliumNodeStatusPatch {
            "alibaba-cloud": outputs.cilium.v2.CiliumNodeStatusAlibabaCloudPatch;
            azure: outputs.cilium.v2.CiliumNodeStatusAzurePatch;
            eni: outputs.cilium.v2.CiliumNodeStatusEniPatch;
            ipam: outputs.cilium.v2.CiliumNodeStatusIpamPatch;
        }

    }

    export namespace v2alpha1 {
        /**
         * CiliumBGPAdvertisement is the Schema for the ciliumbgpadvertisements API
         */
        export interface CiliumBGPAdvertisement {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPAdvertisement";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpec;
        }

        export interface CiliumBGPAdvertisementSpec {
            /**
             * Advertisements is a list of BGP advertisements.
             */
            advertisements: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisements[];
        }

        /**
         * BGPAdvertisement defines which routes Cilium should advertise to BGP peers. Optionally, additional attributes can be
         * set to the advertised routes.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisements {
            /**
             * AdvertisementType defines type of advertisement which has to be advertised.
             */
            advertisementType: string;
            attributes: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsAttributes;
            selector: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsSelector;
            service: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsService;
        }

        /**
         * Attributes defines additional attributes to set to the advertised routes.
         * If not specified, no additional attributes are set.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsAttributes {
            communities: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsAttributesCommunities;
            /**
             * LocalPreference sets the local preference attribute in the route.
             * If not specified, no local preference attribute is set.
             */
            localPreference: number;
        }

        /**
         * Communities sets the community attributes in the route.
         * If not specified, no community attribute is set.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsAttributesCommunities {
            /**
             * Large holds a list of the BGP Large Communities Attribute (RFC 8092) values.
             */
            large: string[];
            /**
             * Standard holds a list of "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as numeric values.
             */
            standard: string[];
            /**
             * WellKnown holds a list "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as
             * well-known string aliases to their numeric values.
             */
            wellKnown: string[];
        }

        /**
         * Communities sets the community attributes in the route.
         * If not specified, no community attribute is set.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsAttributesCommunitiesPatch {
            /**
             * Large holds a list of the BGP Large Communities Attribute (RFC 8092) values.
             */
            large: string[];
            /**
             * Standard holds a list of "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as numeric values.
             */
            standard: string[];
            /**
             * WellKnown holds a list "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as
             * well-known string aliases to their numeric values.
             */
            wellKnown: string[];
        }

        /**
         * Attributes defines additional attributes to set to the advertised routes.
         * If not specified, no additional attributes are set.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsAttributesPatch {
            communities: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsAttributesCommunitiesPatch;
            /**
             * LocalPreference sets the local preference attribute in the route.
             * If not specified, no local preference attribute is set.
             */
            localPreference: number;
        }

        /**
         * BGPAdvertisement defines which routes Cilium should advertise to BGP peers. Optionally, additional attributes can be
         * set to the advertised routes.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsPatch {
            /**
             * AdvertisementType defines type of advertisement which has to be advertised.
             */
            advertisementType: string;
            attributes: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsAttributesPatch;
            selector: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsSelectorPatch;
            service: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsServicePatch;
        }

        /**
         * Selector is a label selector to select objects of the type specified by AdvertisementType.
         * If not specified, no objects of the type specified by AdvertisementType are selected for advertisement.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * Selector is a label selector to select objects of the type specified by AdvertisementType.
         * If not specified, no objects of the type specified by AdvertisementType are selected for advertisement.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Service defines configuration options for advertisementType service.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsService {
            /**
             * Addresses is a list of service address types which needs to be advertised via BGP.
             */
            addresses: string[];
        }

        /**
         * Service defines configuration options for advertisementType service.
         */
        export interface CiliumBGPAdvertisementSpecAdvertisementsServicePatch {
            /**
             * Addresses is a list of service address types which needs to be advertised via BGP.
             */
            addresses: string[];
        }

        export interface CiliumBGPAdvertisementSpecPatch {
            /**
             * Advertisements is a list of BGP advertisements.
             */
            advertisements: outputs.cilium.v2alpha1.CiliumBGPAdvertisementSpecAdvertisementsPatch[];
        }

        /**
         * CiliumBGPClusterConfig is the Schema for the CiliumBGPClusterConfig API
         */
        export interface CiliumBGPClusterConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPClusterConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpec;
            status: outputs.cilium.v2alpha1.CiliumBGPClusterConfigStatus;
        }

        /**
         * Spec defines the desired cluster configuration of the BGP control plane.
         */
        export interface CiliumBGPClusterConfigSpec {
            /**
             * A list of CiliumBGPInstance(s) which instructs
             * the BGP control plane how to instantiate virtual BGP routers.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstances[];
            nodeSelector: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecNodeSelector;
        }

        export interface CiliumBGPClusterConfigSpecBgpInstances {
            /**
             * LocalASN is the ASN of this BGP instance.
             * Supports extended 32bit ASNs.
             */
            localASN: number;
            /**
             * Name is the name of the BGP instance. It is a unique identifier for the BGP instance
             * within the cluster configuration.
             */
            name: string;
            /**
             * Peers is a list of neighboring BGP peers for this virtual router
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstancesPeers[];
        }

        export interface CiliumBGPClusterConfigSpecBgpInstancesPatch {
            /**
             * LocalASN is the ASN of this BGP instance.
             * Supports extended 32bit ASNs.
             */
            localASN: number;
            /**
             * Name is the name of the BGP instance. It is a unique identifier for the BGP instance
             * within the cluster configuration.
             */
            name: string;
            /**
             * Peers is a list of neighboring BGP peers for this virtual router
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstancesPeersPatch[];
        }

        export interface CiliumBGPClusterConfigSpecBgpInstancesPeers {
            /**
             * Name is the name of the BGP peer. It is a unique identifier for the peer within the BGP instance.
             */
            name: string;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs.
             *
             * If peerASN is 0, the BGP OPEN message validation of ASN will be disabled and
             * ASN will be determined based on peer's OPEN message.
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             * Supports IPv4 and IPv6 addresses.
             */
            peerAddress: string;
            peerConfigRef: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstancesPeersPeerConfigRef;
        }

        export interface CiliumBGPClusterConfigSpecBgpInstancesPeersPatch {
            /**
             * Name is the name of the BGP peer. It is a unique identifier for the peer within the BGP instance.
             */
            name: string;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs.
             *
             * If peerASN is 0, the BGP OPEN message validation of ASN will be disabled and
             * ASN will be determined based on peer's OPEN message.
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             * Supports IPv4 and IPv6 addresses.
             */
            peerAddress: string;
            peerConfigRef: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstancesPeersPeerConfigRefPatch;
        }

        /**
         * PeerConfigRef is a reference to a peer configuration resource.
         * If not specified, the default BGP configuration is used for this peer.
         */
        export interface CiliumBGPClusterConfigSpecBgpInstancesPeersPeerConfigRef {
            /**
             * Group is the group of the peer config resource.
             * If not specified, the default of "cilium.io" is used.
             */
            group: string;
            /**
             * Kind is the kind of the peer config resource.
             * If not specified, the default of "CiliumBGPPeerConfig" is used.
             */
            kind: string;
            /**
             * Name is the name of the peer config resource.
             * Name refers to the name of a Kubernetes object (typically a CiliumBGPPeerConfig).
             */
            name: string;
        }

        /**
         * PeerConfigRef is a reference to a peer configuration resource.
         * If not specified, the default BGP configuration is used for this peer.
         */
        export interface CiliumBGPClusterConfigSpecBgpInstancesPeersPeerConfigRefPatch {
            /**
             * Group is the group of the peer config resource.
             * If not specified, the default of "cilium.io" is used.
             */
            group: string;
            /**
             * Kind is the kind of the peer config resource.
             * If not specified, the default of "CiliumBGPPeerConfig" is used.
             */
            kind: string;
            /**
             * Name is the name of the peer config resource.
             * Name refers to the name of a Kubernetes object (typically a CiliumBGPPeerConfig).
             */
            name: string;
        }

        /**
         * NodeSelector selects a group of nodes where this BGP Cluster
         * config applies.
         * If empty / nil this config applies to all nodes.
         */
        export interface CiliumBGPClusterConfigSpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPClusterConfigSpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPClusterConfigSpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects a group of nodes where this BGP Cluster
         * config applies.
         * If empty / nil this config applies to all nodes.
         */
        export interface CiliumBGPClusterConfigSpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec defines the desired cluster configuration of the BGP control plane.
         */
        export interface CiliumBGPClusterConfigSpecPatch {
            /**
             * A list of CiliumBGPInstance(s) which instructs
             * the BGP control plane how to instantiate virtual BGP routers.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecBgpInstancesPatch[];
            nodeSelector: outputs.cilium.v2alpha1.CiliumBGPClusterConfigSpecNodeSelectorPatch;
        }

        /**
         * Status is a running status of the cluster configuration
         */
        export interface CiliumBGPClusterConfigStatus {
            /**
             * The current conditions of the CiliumBGPClusterConfig
             */
            conditions: outputs.cilium.v2alpha1.CiliumBGPClusterConfigStatusConditions[];
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumBGPClusterConfigStatusConditions {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumBGPClusterConfigStatusConditionsPatch {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Status is a running status of the cluster configuration
         */
        export interface CiliumBGPClusterConfigStatusPatch {
            /**
             * The current conditions of the CiliumBGPClusterConfig
             */
            conditions: outputs.cilium.v2alpha1.CiliumBGPClusterConfigStatusConditionsPatch[];
        }

        /**
         * CiliumBGPNodeConfig is node local configuration for BGP agent. Name of the object should be node name.
         * This resource will be created by Cilium operator and is read-only for the users.
         */
        export interface CiliumBGPNodeConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPNodeConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpec;
            status: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatus;
        }

        /**
         * CiliumBGPNodeConfigOverride specifies configuration overrides for a CiliumBGPNodeConfig.
         * It allows fine-tuning of BGP behavior on a per-node basis. For the override to be effective,
         * the names in CiliumBGPNodeConfigOverride and CiliumBGPNodeConfig must match exactly. This
         * matching ensures that specific node configurations are applied correctly and only where intended.
         */
        export interface CiliumBGPNodeConfigOverride {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPNodeConfigOverride";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPNodeConfigOverrideSpec;
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPNodeConfigOverride.
         */
        export interface CiliumBGPNodeConfigOverrideSpec {
            /**
             * BGPInstances is a list of BGP instances to override.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigOverrideSpecBgpInstances[];
        }

        /**
         * CiliumBGPNodeConfigInstanceOverride defines configuration options which can be overridden for a specific BGP instance.
         */
        export interface CiliumBGPNodeConfigOverrideSpecBgpInstances {
            /**
             * LocalPort is port to use for this BGP instance.
             */
            localPort: number;
            /**
             * Name is the name of the BGP instance for which the configuration is overridden.
             */
            name: string;
            /**
             * Peers is a list of peer configurations to override.
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigOverrideSpecBgpInstancesPeers[];
            /**
             * RouterID is BGP router id to use for this instance. It must be unique across all BGP instances.
             */
            routerID: string;
        }

        /**
         * CiliumBGPNodeConfigInstanceOverride defines configuration options which can be overridden for a specific BGP instance.
         */
        export interface CiliumBGPNodeConfigOverrideSpecBgpInstancesPatch {
            /**
             * LocalPort is port to use for this BGP instance.
             */
            localPort: number;
            /**
             * Name is the name of the BGP instance for which the configuration is overridden.
             */
            name: string;
            /**
             * Peers is a list of peer configurations to override.
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigOverrideSpecBgpInstancesPeersPatch[];
            /**
             * RouterID is BGP router id to use for this instance. It must be unique across all BGP instances.
             */
            routerID: string;
        }

        /**
         * CiliumBGPNodeConfigPeerOverride defines configuration options which can be overridden for a specific peer.
         */
        export interface CiliumBGPNodeConfigOverrideSpecBgpInstancesPeers {
            /**
             * LocalAddress is the IP address to use for connecting to this peer.
             */
            localAddress: string;
            /**
             * LocalPort is source port to use for connecting to this peer.
             */
            localPort: number;
            /**
             * Name is the name of the peer for which the configuration is overridden.
             */
            name: string;
        }

        /**
         * CiliumBGPNodeConfigPeerOverride defines configuration options which can be overridden for a specific peer.
         */
        export interface CiliumBGPNodeConfigOverrideSpecBgpInstancesPeersPatch {
            /**
             * LocalAddress is the IP address to use for connecting to this peer.
             */
            localAddress: string;
            /**
             * LocalPort is source port to use for connecting to this peer.
             */
            localPort: number;
            /**
             * Name is the name of the peer for which the configuration is overridden.
             */
            name: string;
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPNodeConfigOverride.
         */
        export interface CiliumBGPNodeConfigOverrideSpecPatch {
            /**
             * BGPInstances is a list of BGP instances to override.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigOverrideSpecBgpInstancesPatch[];
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPNodeConfig.
         */
        export interface CiliumBGPNodeConfigSpec {
            /**
             * BGPInstances is a list of BGP router instances on the node.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstances[];
        }

        /**
         * CiliumBGPNodeInstance is a single BGP router instance configuration on the node.
         */
        export interface CiliumBGPNodeConfigSpecBgpInstances {
            /**
             * LocalASN is the ASN of this virtual router.
             * Supports extended 32bit ASNs.
             */
            localASN: number;
            /**
             * LocalPort is the port on which the BGP daemon listens for incoming connections.
             *
             * If not specified, BGP instance will not listen for incoming connections.
             */
            localPort: number;
            /**
             * Name is the name of the BGP instance. This name is used to identify the BGP instance on the node.
             */
            name: string;
            /**
             * Peers is a list of neighboring BGP peers for this virtual router
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstancesPeers[];
            /**
             * RouterID is the BGP router ID of this virtual router.
             * This configuration is derived from CiliumBGPNodeConfigOverride resource.
             *
             * If not specified, the router ID will be derived from the node local address.
             */
            routerID: string;
        }

        /**
         * CiliumBGPNodeInstance is a single BGP router instance configuration on the node.
         */
        export interface CiliumBGPNodeConfigSpecBgpInstancesPatch {
            /**
             * LocalASN is the ASN of this virtual router.
             * Supports extended 32bit ASNs.
             */
            localASN: number;
            /**
             * LocalPort is the port on which the BGP daemon listens for incoming connections.
             *
             * If not specified, BGP instance will not listen for incoming connections.
             */
            localPort: number;
            /**
             * Name is the name of the BGP instance. This name is used to identify the BGP instance on the node.
             */
            name: string;
            /**
             * Peers is a list of neighboring BGP peers for this virtual router
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstancesPeersPatch[];
            /**
             * RouterID is the BGP router ID of this virtual router.
             * This configuration is derived from CiliumBGPNodeConfigOverride resource.
             *
             * If not specified, the router ID will be derived from the node local address.
             */
            routerID: string;
        }

        export interface CiliumBGPNodeConfigSpecBgpInstancesPeers {
            /**
             * LocalAddress is the IP address of the local interface to use for the peering session.
             * This configuration is derived from CiliumBGPNodeConfigOverride resource. If not specified, the local address will be used for setting up peering.
             */
            localAddress: string;
            /**
             * Name is the name of the BGP peer. This name is used to identify the BGP peer for the BGP instance.
             */
            name: string;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             * Supports IPv4 and IPv6 addresses.
             */
            peerAddress: string;
            peerConfigRef: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstancesPeersPeerConfigRef;
        }

        export interface CiliumBGPNodeConfigSpecBgpInstancesPeersPatch {
            /**
             * LocalAddress is the IP address of the local interface to use for the peering session.
             * This configuration is derived from CiliumBGPNodeConfigOverride resource. If not specified, the local address will be used for setting up peering.
             */
            localAddress: string;
            /**
             * Name is the name of the BGP peer. This name is used to identify the BGP peer for the BGP instance.
             */
            name: string;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             * Supports IPv4 and IPv6 addresses.
             */
            peerAddress: string;
            peerConfigRef: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstancesPeersPeerConfigRefPatch;
        }

        /**
         * PeerConfigRef is a reference to a peer configuration resource.
         * If not specified, the default BGP configuration is used for this peer.
         */
        export interface CiliumBGPNodeConfigSpecBgpInstancesPeersPeerConfigRef {
            /**
             * Group is the group of the peer config resource.
             * If not specified, the default of "cilium.io" is used.
             */
            group: string;
            /**
             * Kind is the kind of the peer config resource.
             * If not specified, the default of "CiliumBGPPeerConfig" is used.
             */
            kind: string;
            /**
             * Name is the name of the peer config resource.
             * Name refers to the name of a Kubernetes object (typically a CiliumBGPPeerConfig).
             */
            name: string;
        }

        /**
         * PeerConfigRef is a reference to a peer configuration resource.
         * If not specified, the default BGP configuration is used for this peer.
         */
        export interface CiliumBGPNodeConfigSpecBgpInstancesPeersPeerConfigRefPatch {
            /**
             * Group is the group of the peer config resource.
             * If not specified, the default of "cilium.io" is used.
             */
            group: string;
            /**
             * Kind is the kind of the peer config resource.
             * If not specified, the default of "CiliumBGPPeerConfig" is used.
             */
            kind: string;
            /**
             * Name is the name of the peer config resource.
             * Name refers to the name of a Kubernetes object (typically a CiliumBGPPeerConfig).
             */
            name: string;
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPNodeConfig.
         */
        export interface CiliumBGPNodeConfigSpecPatch {
            /**
             * BGPInstances is a list of BGP router instances on the node.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigSpecBgpInstancesPatch[];
        }

        /**
         * Status is the most recently observed status of the CiliumBGPNodeConfig.
         */
        export interface CiliumBGPNodeConfigStatus {
            /**
             * BGPInstances is the status of the BGP instances on the node.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstances[];
        }

        export interface CiliumBGPNodeConfigStatusBgpInstances {
            /**
             * LocalASN is the ASN of this BGP instance.
             */
            localASN: number;
            /**
             * Name is the name of the BGP instance. This name is used to identify the BGP instance on the node.
             */
            name: string;
            /**
             * PeerStatuses is the state of the BGP peers for this BGP instance.
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeers[];
        }

        export interface CiliumBGPNodeConfigStatusBgpInstancesPatch {
            /**
             * LocalASN is the ASN of this BGP instance.
             */
            localASN: number;
            /**
             * Name is the name of the BGP instance. This name is used to identify the BGP instance on the node.
             */
            name: string;
            /**
             * PeerStatuses is the state of the BGP peers for this BGP instance.
             */
            peers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeersPatch[];
        }

        /**
         * CiliumBGPNodePeerStatus is the status of a BGP peer.
         */
        export interface CiliumBGPNodeConfigStatusBgpInstancesPeers {
            /**
             * EstablishedTime is the time when the peering session was established.
             * It is represented in RFC3339 form and is in UTC.
             */
            establishedTime: string;
            /**
             * Name is the name of the BGP peer.
             */
            name: string;
            /**
             * PeerASN is the ASN of the neighbor.
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             */
            peerAddress: string;
            /**
             * PeeringState is last known state of the peering session.
             */
            peeringState: string;
            /**
             * RouteCount is the number of routes exchanged with this peer per AFI/SAFI.
             */
            routeCount: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeersRouteCount[];
            timers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeersTimers;
        }

        /**
         * CiliumBGPNodePeerStatus is the status of a BGP peer.
         */
        export interface CiliumBGPNodeConfigStatusBgpInstancesPeersPatch {
            /**
             * EstablishedTime is the time when the peering session was established.
             * It is represented in RFC3339 form and is in UTC.
             */
            establishedTime: string;
            /**
             * Name is the name of the BGP peer.
             */
            name: string;
            /**
             * PeerASN is the ASN of the neighbor.
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the neighbor.
             */
            peerAddress: string;
            /**
             * PeeringState is last known state of the peering session.
             */
            peeringState: string;
            /**
             * RouteCount is the number of routes exchanged with this peer per AFI/SAFI.
             */
            routeCount: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeersRouteCountPatch[];
            timers: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPeersTimersPatch;
        }

        export interface CiliumBGPNodeConfigStatusBgpInstancesPeersRouteCount {
            /**
             * Advertised is the number of routes advertised to this peer.
             */
            advertised: number;
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Received is the number of routes received from this peer.
             */
            received: number;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        export interface CiliumBGPNodeConfigStatusBgpInstancesPeersRouteCountPatch {
            /**
             * Advertised is the number of routes advertised to this peer.
             */
            advertised: number;
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Received is the number of routes received from this peer.
             */
            received: number;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        /**
         * Timers is the state of the negotiated BGP timers for this peer.
         */
        export interface CiliumBGPNodeConfigStatusBgpInstancesPeersTimers {
            /**
             * AppliedHoldTimeSeconds is the negotiated hold time for this peer.
             */
            appliedHoldTimeSeconds: number;
            /**
             * AppliedKeepaliveSeconds is the negotiated keepalive time for this peer.
             */
            appliedKeepaliveSeconds: number;
        }

        /**
         * Timers is the state of the negotiated BGP timers for this peer.
         */
        export interface CiliumBGPNodeConfigStatusBgpInstancesPeersTimersPatch {
            /**
             * AppliedHoldTimeSeconds is the negotiated hold time for this peer.
             */
            appliedHoldTimeSeconds: number;
            /**
             * AppliedKeepaliveSeconds is the negotiated keepalive time for this peer.
             */
            appliedKeepaliveSeconds: number;
        }

        /**
         * Status is the most recently observed status of the CiliumBGPNodeConfig.
         */
        export interface CiliumBGPNodeConfigStatusPatch {
            /**
             * BGPInstances is the status of the BGP instances on the node.
             */
            bgpInstances: outputs.cilium.v2alpha1.CiliumBGPNodeConfigStatusBgpInstancesPatch[];
        }

        export interface CiliumBGPPeerConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPPeerConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpec;
            status: outputs.cilium.v2alpha1.CiliumBGPPeerConfigStatus;
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPPeerConfig.
         */
        export interface CiliumBGPPeerConfigSpec {
            /**
             * AuthSecretRef is the name of the secret to use to fetch a TCP
             * authentication password for this peer.
             *
             * If not specified, no authentication is used.
             */
            authSecretRef: string;
            /**
             * EBGPMultihopTTL controls the multi-hop feature for eBGP peers.
             * Its value defines the Time To Live (TTL) value used in BGP
             * packets sent to the peer.
             *
             * If not specified, EBGP multihop is disabled. This field is ignored for iBGP neighbors.
             */
            ebgpMultihop: number;
            /**
             * Families, if provided, defines a set of AFI/SAFIs the speaker will
             * negotiate with it's peer.
             *
             * If not specified, the default families of IPv6/unicast and IPv4/unicast will be created.
             */
            families: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamilies[];
            gracefulRestart: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecGracefulRestart;
            timers: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecTimers;
            transport: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecTransport;
        }

        /**
         * CiliumBGPFamilyWithAdverts represents a AFI/SAFI address family pair along with reference to BGP Advertisements.
         */
        export interface CiliumBGPPeerConfigSpecFamilies {
            advertisements: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamiliesAdvertisements;
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        /**
         * Advertisements selects group of BGP Advertisement(s) to advertise for this family.
         *
         * If not specified, no advertisements are sent for this family.
         *
         * This field is ignored in CiliumBGPNeighbor which is used in CiliumBGPPeeringPolicy.
         * Use CiliumBGPPeeringPolicy advertisement options instead.
         */
        export interface CiliumBGPPeerConfigSpecFamiliesAdvertisements {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamiliesAdvertisementsMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeerConfigSpecFamiliesAdvertisementsMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeerConfigSpecFamiliesAdvertisementsMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * Advertisements selects group of BGP Advertisement(s) to advertise for this family.
         *
         * If not specified, no advertisements are sent for this family.
         *
         * This field is ignored in CiliumBGPNeighbor which is used in CiliumBGPPeeringPolicy.
         * Use CiliumBGPPeeringPolicy advertisement options instead.
         */
        export interface CiliumBGPPeerConfigSpecFamiliesAdvertisementsPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamiliesAdvertisementsMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CiliumBGPFamilyWithAdverts represents a AFI/SAFI address family pair along with reference to BGP Advertisements.
         */
        export interface CiliumBGPPeerConfigSpecFamiliesPatch {
            advertisements: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamiliesAdvertisementsPatch;
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        /**
         * GracefulRestart defines graceful restart parameters which are negotiated
         * with this peer.
         *
         * If not specified, the graceful restart capability is disabled.
         */
        export interface CiliumBGPPeerConfigSpecGracefulRestart {
            /**
             * Enabled flag, when set enables graceful restart capability.
             */
            enabled: boolean;
            /**
             * RestartTimeSeconds is the estimated time it will take for the BGP
             * session to be re-established with peer after a restart.
             * After this period, peer will remove stale routes. This is
             * described RFC 4724 section 4.2.
             */
            restartTimeSeconds: number;
        }

        /**
         * GracefulRestart defines graceful restart parameters which are negotiated
         * with this peer.
         *
         * If not specified, the graceful restart capability is disabled.
         */
        export interface CiliumBGPPeerConfigSpecGracefulRestartPatch {
            /**
             * Enabled flag, when set enables graceful restart capability.
             */
            enabled: boolean;
            /**
             * RestartTimeSeconds is the estimated time it will take for the BGP
             * session to be re-established with peer after a restart.
             * After this period, peer will remove stale routes. This is
             * described RFC 4724 section 4.2.
             */
            restartTimeSeconds: number;
        }

        /**
         * Spec is the specification of the desired behavior of the CiliumBGPPeerConfig.
         */
        export interface CiliumBGPPeerConfigSpecPatch {
            /**
             * AuthSecretRef is the name of the secret to use to fetch a TCP
             * authentication password for this peer.
             *
             * If not specified, no authentication is used.
             */
            authSecretRef: string;
            /**
             * EBGPMultihopTTL controls the multi-hop feature for eBGP peers.
             * Its value defines the Time To Live (TTL) value used in BGP
             * packets sent to the peer.
             *
             * If not specified, EBGP multihop is disabled. This field is ignored for iBGP neighbors.
             */
            ebgpMultihop: number;
            /**
             * Families, if provided, defines a set of AFI/SAFIs the speaker will
             * negotiate with it's peer.
             *
             * If not specified, the default families of IPv6/unicast and IPv4/unicast will be created.
             */
            families: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecFamiliesPatch[];
            gracefulRestart: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecGracefulRestartPatch;
            timers: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecTimersPatch;
            transport: outputs.cilium.v2alpha1.CiliumBGPPeerConfigSpecTransportPatch;
        }

        /**
         * Timers defines the BGP timers for the peer.
         *
         * If not specified, the default timers are used.
         */
        export interface CiliumBGPPeerConfigSpecTimers {
            /**
             * ConnectRetryTimeSeconds defines the initial value for the BGP ConnectRetryTimer (RFC 4271, Section 8).
             *
             * If not specified, defaults to 120 seconds.
             */
            connectRetryTimeSeconds: number;
            /**
             * HoldTimeSeconds defines the initial value for the BGP HoldTimer (RFC 4271, Section 4.2).
             * Updating this value will cause a session reset.
             *
             * If not specified, defaults to 90 seconds.
             */
            holdTimeSeconds: number;
            /**
             * KeepaliveTimeSeconds defines the initial value for the BGP KeepaliveTimer (RFC 4271, Section 8).
             * It can not be larger than HoldTimeSeconds. Updating this value will cause a session reset.
             *
             * If not specified, defaults to 30 seconds.
             */
            keepAliveTimeSeconds: number;
        }

        /**
         * Timers defines the BGP timers for the peer.
         *
         * If not specified, the default timers are used.
         */
        export interface CiliumBGPPeerConfigSpecTimersPatch {
            /**
             * ConnectRetryTimeSeconds defines the initial value for the BGP ConnectRetryTimer (RFC 4271, Section 8).
             *
             * If not specified, defaults to 120 seconds.
             */
            connectRetryTimeSeconds: number;
            /**
             * HoldTimeSeconds defines the initial value for the BGP HoldTimer (RFC 4271, Section 4.2).
             * Updating this value will cause a session reset.
             *
             * If not specified, defaults to 90 seconds.
             */
            holdTimeSeconds: number;
            /**
             * KeepaliveTimeSeconds defines the initial value for the BGP KeepaliveTimer (RFC 4271, Section 8).
             * It can not be larger than HoldTimeSeconds. Updating this value will cause a session reset.
             *
             * If not specified, defaults to 30 seconds.
             */
            keepAliveTimeSeconds: number;
        }

        /**
         * Transport defines the BGP transport parameters for the peer.
         *
         * If not specified, the default transport parameters are used.
         */
        export interface CiliumBGPPeerConfigSpecTransport {
            /**
             * Deprecated
             * LocalPort is the local port to be used for the BGP session.
             *
             * If not specified, ephemeral port will be picked to initiate a connection.
             *
             * This field is deprecated and will be removed in a future release.
             * Local port configuration is unnecessary and is not recommended.
             */
            localPort: number;
            /**
             * PeerPort is the peer port to be used for the BGP session.
             *
             * If not specified, defaults to TCP port 179.
             */
            peerPort: number;
        }

        /**
         * Transport defines the BGP transport parameters for the peer.
         *
         * If not specified, the default transport parameters are used.
         */
        export interface CiliumBGPPeerConfigSpecTransportPatch {
            /**
             * Deprecated
             * LocalPort is the local port to be used for the BGP session.
             *
             * If not specified, ephemeral port will be picked to initiate a connection.
             *
             * This field is deprecated and will be removed in a future release.
             * Local port configuration is unnecessary and is not recommended.
             */
            localPort: number;
            /**
             * PeerPort is the peer port to be used for the BGP session.
             *
             * If not specified, defaults to TCP port 179.
             */
            peerPort: number;
        }

        /**
         * Status is the running status of the CiliumBGPPeerConfig
         */
        export interface CiliumBGPPeerConfigStatus {
            /**
             * The current conditions of the CiliumBGPPeerConfig
             */
            conditions: outputs.cilium.v2alpha1.CiliumBGPPeerConfigStatusConditions[];
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumBGPPeerConfigStatusConditions {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumBGPPeerConfigStatusConditionsPatch {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Status is the running status of the CiliumBGPPeerConfig
         */
        export interface CiliumBGPPeerConfigStatusPatch {
            /**
             * The current conditions of the CiliumBGPPeerConfig
             */
            conditions: outputs.cilium.v2alpha1.CiliumBGPPeerConfigStatusConditionsPatch[];
        }

        /**
         * CiliumBGPPeeringPolicy is a Kubernetes third-party resource for instructing
         * Cilium's BGP control plane to create virtual BGP routers.
         */
        export interface CiliumBGPPeeringPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumBGPPeeringPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpec;
        }

        /**
         * Spec is a human readable description of a BGP peering policy
         */
        export interface CiliumBGPPeeringPolicySpec {
            nodeSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecNodeSelector;
            /**
             * A list of CiliumBGPVirtualRouter(s) which instructs
             * the BGP control plane how to instantiate virtual BGP routers.
             */
            virtualRouters: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRouters[];
        }

        /**
         * NodeSelector selects a group of nodes where this BGP Peering
         * Policy applies.
         *
         * If empty / nil this policy applies to all nodes.
         */
        export interface CiliumBGPPeeringPolicySpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects a group of nodes where this BGP Peering
         * Policy applies.
         *
         * If empty / nil this policy applies to all nodes.
         */
        export interface CiliumBGPPeeringPolicySpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is a human readable description of a BGP peering policy
         */
        export interface CiliumBGPPeeringPolicySpecPatch {
            nodeSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecNodeSelectorPatch;
            /**
             * A list of CiliumBGPVirtualRouter(s) which instructs
             * the BGP control plane how to instantiate virtual BGP routers.
             */
            virtualRouters: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersPatch[];
        }

        /**
         * CiliumBGPVirtualRouter defines a discrete BGP virtual router configuration.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRouters {
            /**
             * ExportPodCIDR determines whether to export the Node's private CIDR block
             * to the configured neighbors.
             */
            exportPodCIDR: boolean;
            /**
             * LocalASN is the ASN of this virtual router.
             * Supports extended 32bit ASNs
             */
            localASN: number;
            /**
             * Neighbors is a list of neighboring BGP peers for this virtual router
             */
            neighbors: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighbors[];
            podIPPoolSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelector;
            /**
             * ServiceAdvertisements selects a group of BGP Advertisement(s) to advertise
             * for the selected services.
             */
            serviceAdvertisements: string[];
            serviceSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelector;
        }

        /**
         * CiliumBGPNeighbor is a neighboring peer for use in a
         * CiliumBGPVirtualRouter configuration.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighbors {
            /**
             * AdvertisedPathAttributes can be used to apply additional path attributes
             * to selected routes when advertising them to the peer.
             * If empty / nil, no additional path attributes are advertised.
             */
            advertisedPathAttributes: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributes[];
            /**
             * AuthSecretRef is the name of the secret to use to fetch a TCP
             * authentication password for this peer.
             */
            authSecretRef: string;
            /**
             * ConnectRetryTimeSeconds defines the initial value for the BGP ConnectRetryTimer (RFC 4271, Section 8).
             */
            connectRetryTimeSeconds: number;
            /**
             * EBGPMultihopTTL controls the multi-hop feature for eBGP peers.
             * Its value defines the Time To Live (TTL) value used in BGP packets sent to the neighbor.
             * The value 1 implies that eBGP multi-hop feature is disabled (only a single hop is allowed).
             * This field is ignored for iBGP peers.
             */
            eBGPMultihopTTL: number;
            /**
             * Families, if provided, defines a set of AFI/SAFIs the speaker will
             * negotiate with it's peer.
             *
             * If this slice is not provided the default families of IPv6 and IPv4 will
             * be provided.
             */
            families: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsFamilies[];
            gracefulRestart: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsGracefulRestart;
            /**
             * HoldTimeSeconds defines the initial value for the BGP HoldTimer (RFC 4271, Section 4.2).
             * Updating this value will cause a session reset.
             */
            holdTimeSeconds: number;
            /**
             * KeepaliveTimeSeconds defines the initial value for the BGP KeepaliveTimer (RFC 4271, Section 8).
             * It can not be larger than HoldTimeSeconds. Updating this value will cause a session reset.
             */
            keepAliveTimeSeconds: number;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the peer.
             * This must be in CIDR notation and use a /32 to express
             * a single host.
             */
            peerAddress: string;
            /**
             * PeerPort is the TCP port of the peer. 1-65535 is the range of
             * valid port numbers that can be specified. If unset, defaults to 179.
             */
            peerPort: number;
        }

        /**
         * CiliumBGPPathAttributes can be used to apply additional path attributes
         * to matched routes when advertising them to a BGP peer.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributes {
            communities: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesCommunities;
            /**
             * LocalPreference defines the preference value advertised in the BGP Local Preference path attribute.
             * As Local Preference is only valid for iBGP peers, this value will be ignored for eBGP peers
             * (no Local Preference path attribute will be advertised).
             * If nil / not set, the default Local Preference of 100 will be advertised in
             * the Local Preference path attribute for iBGP peers.
             */
            localPreference: number;
            selector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelector;
            /**
             * SelectorType defines the object type on which the Selector applies:
             * - For "PodCIDR" the Selector matches k8s CiliumNode resources
             *   (path attributes apply to routes announced for PodCIDRs of selected CiliumNodes.
             *   Only affects routes of cluster scope / Kubernetes IPAM CIDRs, not Multi-Pool IPAM CIDRs.
             * - For "CiliumLoadBalancerIPPool" the Selector matches CiliumLoadBalancerIPPool custom resources
             *   (path attributes apply to routes announced for selected CiliumLoadBalancerIPPools).
             * - For "CiliumPodIPPool" the Selector matches CiliumPodIPPool custom resources
             *   (path attributes apply to routes announced for allocated CIDRs of selected CiliumPodIPPools).
             */
            selectorType: string;
        }

        /**
         * Communities defines a set of community values advertised in the supported BGP Communities path attributes.
         * If nil / not set, no BGP Communities path attribute will be advertised.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesCommunities {
            /**
             * Large holds a list of the BGP Large Communities Attribute (RFC 8092) values.
             */
            large: string[];
            /**
             * Standard holds a list of "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as numeric values.
             */
            standard: string[];
            /**
             * WellKnown holds a list "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as
             * well-known string aliases to their numeric values.
             */
            wellKnown: string[];
        }

        /**
         * Communities defines a set of community values advertised in the supported BGP Communities path attributes.
         * If nil / not set, no BGP Communities path attribute will be advertised.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesCommunitiesPatch {
            /**
             * Large holds a list of the BGP Large Communities Attribute (RFC 8092) values.
             */
            large: string[];
            /**
             * Standard holds a list of "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as numeric values.
             */
            standard: string[];
            /**
             * WellKnown holds a list "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as
             * well-known string aliases to their numeric values.
             */
            wellKnown: string[];
        }

        /**
         * CiliumBGPPathAttributes can be used to apply additional path attributes
         * to matched routes when advertising them to a BGP peer.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesPatch {
            communities: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesCommunitiesPatch;
            /**
             * LocalPreference defines the preference value advertised in the BGP Local Preference path attribute.
             * As Local Preference is only valid for iBGP peers, this value will be ignored for eBGP peers
             * (no Local Preference path attribute will be advertised).
             * If nil / not set, the default Local Preference of 100 will be advertised in
             * the Local Preference path attribute for iBGP peers.
             */
            localPreference: number;
            selector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorPatch;
            /**
             * SelectorType defines the object type on which the Selector applies:
             * - For "PodCIDR" the Selector matches k8s CiliumNode resources
             *   (path attributes apply to routes announced for PodCIDRs of selected CiliumNodes.
             *   Only affects routes of cluster scope / Kubernetes IPAM CIDRs, not Multi-Pool IPAM CIDRs.
             * - For "CiliumLoadBalancerIPPool" the Selector matches CiliumLoadBalancerIPPool custom resources
             *   (path attributes apply to routes announced for selected CiliumLoadBalancerIPPools).
             * - For "CiliumPodIPPool" the Selector matches CiliumPodIPPool custom resources
             *   (path attributes apply to routes announced for allocated CIDRs of selected CiliumPodIPPools).
             */
            selectorType: string;
        }

        /**
         * Selector selects a group of objects of the SelectorType
         * resulting into routes that will be announced with the configured Attributes.
         * If nil / not set, all objects of the SelectorType are selected.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * Selector selects a group of objects of the SelectorType
         * resulting into routes that will be announced with the configured Attributes.
         * If nil / not set, all objects of the SelectorType are selected.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CiliumBGPFamily represents a AFI/SAFI address family pair.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsFamilies {
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        /**
         * CiliumBGPFamily represents a AFI/SAFI address family pair.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsFamiliesPatch {
            /**
             * Afi is the Address Family Identifier (AFI) of the family.
             */
            afi: string;
            /**
             * Safi is the Subsequent Address Family Identifier (SAFI) of the family.
             */
            safi: string;
        }

        /**
         * GracefulRestart defines graceful restart parameters which are negotiated
         * with this neighbor. If empty / nil, the graceful restart capability is disabled.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsGracefulRestart {
            /**
             * Enabled flag, when set enables graceful restart capability.
             */
            enabled: boolean;
            /**
             * RestartTimeSeconds is the estimated time it will take for the BGP
             * session to be re-established with peer after a restart.
             * After this period, peer will remove stale routes. This is
             * described RFC 4724 section 4.2.
             */
            restartTimeSeconds: number;
        }

        /**
         * GracefulRestart defines graceful restart parameters which are negotiated
         * with this neighbor. If empty / nil, the graceful restart capability is disabled.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsGracefulRestartPatch {
            /**
             * Enabled flag, when set enables graceful restart capability.
             */
            enabled: boolean;
            /**
             * RestartTimeSeconds is the estimated time it will take for the BGP
             * session to be re-established with peer after a restart.
             * After this period, peer will remove stale routes. This is
             * described RFC 4724 section 4.2.
             */
            restartTimeSeconds: number;
        }

        /**
         * CiliumBGPNeighbor is a neighboring peer for use in a
         * CiliumBGPVirtualRouter configuration.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsPatch {
            /**
             * AdvertisedPathAttributes can be used to apply additional path attributes
             * to selected routes when advertising them to the peer.
             * If empty / nil, no additional path attributes are advertised.
             */
            advertisedPathAttributes: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsAdvertisedPathAttributesPatch[];
            /**
             * AuthSecretRef is the name of the secret to use to fetch a TCP
             * authentication password for this peer.
             */
            authSecretRef: string;
            /**
             * ConnectRetryTimeSeconds defines the initial value for the BGP ConnectRetryTimer (RFC 4271, Section 8).
             */
            connectRetryTimeSeconds: number;
            /**
             * EBGPMultihopTTL controls the multi-hop feature for eBGP peers.
             * Its value defines the Time To Live (TTL) value used in BGP packets sent to the neighbor.
             * The value 1 implies that eBGP multi-hop feature is disabled (only a single hop is allowed).
             * This field is ignored for iBGP peers.
             */
            eBGPMultihopTTL: number;
            /**
             * Families, if provided, defines a set of AFI/SAFIs the speaker will
             * negotiate with it's peer.
             *
             * If this slice is not provided the default families of IPv6 and IPv4 will
             * be provided.
             */
            families: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsFamiliesPatch[];
            gracefulRestart: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsGracefulRestartPatch;
            /**
             * HoldTimeSeconds defines the initial value for the BGP HoldTimer (RFC 4271, Section 4.2).
             * Updating this value will cause a session reset.
             */
            holdTimeSeconds: number;
            /**
             * KeepaliveTimeSeconds defines the initial value for the BGP KeepaliveTimer (RFC 4271, Section 8).
             * It can not be larger than HoldTimeSeconds. Updating this value will cause a session reset.
             */
            keepAliveTimeSeconds: number;
            /**
             * PeerASN is the ASN of the peer BGP router.
             * Supports extended 32bit ASNs
             */
            peerASN: number;
            /**
             * PeerAddress is the IP address of the peer.
             * This must be in CIDR notation and use a /32 to express
             * a single host.
             */
            peerAddress: string;
            /**
             * PeerPort is the TCP port of the peer. 1-65535 is the range of
             * valid port numbers that can be specified. If unset, defaults to 179.
             */
            peerPort: number;
        }

        /**
         * CiliumBGPVirtualRouter defines a discrete BGP virtual router configuration.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersPatch {
            /**
             * ExportPodCIDR determines whether to export the Node's private CIDR block
             * to the configured neighbors.
             */
            exportPodCIDR: boolean;
            /**
             * LocalASN is the ASN of this virtual router.
             * Supports extended 32bit ASNs
             */
            localASN: number;
            /**
             * Neighbors is a list of neighboring BGP peers for this virtual router
             */
            neighbors: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersNeighborsPatch[];
            podIPPoolSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorPatch;
            /**
             * ServiceAdvertisements selects a group of BGP Advertisement(s) to advertise
             * for the selected services.
             */
            serviceAdvertisements: string[];
            serviceSelector: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorPatch;
        }

        /**
         * PodIPPoolSelector selects CiliumPodIPPools based on labels. The virtual
         * router will announce allocated CIDRs of matching CiliumPodIPPools.
         *
         * If empty / nil no CiliumPodIPPools will be announced.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * PodIPPoolSelector selects CiliumPodIPPools based on labels. The virtual
         * router will announce allocated CIDRs of matching CiliumPodIPPools.
         *
         * If empty / nil no CiliumPodIPPools will be announced.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersPodIPPoolSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * ServiceSelector selects a group of load balancer services which this
         * virtual router will announce. The loadBalancerClass for a service must
         * be nil or specify a class supported by Cilium, e.g. "io.cilium/bgp-control-plane".
         * Refer to the following document for additional details regarding load balancer
         * classes:
         *
         *   https://kubernetes.io/docs/concepts/services-networking/service/#load-balancer-class
         *
         * If empty / nil no services will be announced.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector selects a group of load balancer services which this
         * virtual router will announce. The loadBalancerClass for a service must
         * be nil or specify a class supported by Cilium, e.g. "io.cilium/bgp-control-plane".
         * Refer to the following document for additional details regarding load balancer
         * classes:
         *
         *   https://kubernetes.io/docs/concepts/services-networking/service/#load-balancer-class
         *
         * If empty / nil no services will be announced.
         */
        export interface CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumBGPPeeringPolicySpecVirtualRoutersServiceSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * CiliumCIDRGroup is a list of external CIDRs (i.e: CIDRs selecting peers
         * outside the clusters) that can be referenced as a single entity from
         * CiliumNetworkPolicies.
         */
        export interface CiliumCIDRGroup {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumCIDRGroup";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumCIDRGroupSpec;
        }

        export interface CiliumCIDRGroupSpec {
            /**
             * ExternalCIDRs is a list of CIDRs selecting peers outside the clusters.
             */
            externalCIDRs: string[];
        }

        export interface CiliumCIDRGroupSpecPatch {
            /**
             * ExternalCIDRs is a list of CIDRs selecting peers outside the clusters.
             */
            externalCIDRs: string[];
        }

        /**
         * CiliumEndpointSlice contains a group of CoreCiliumendpoints.
         */
        export interface CiliumEndpointSlice {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Endpoints is a list of coreCEPs packed in a CiliumEndpointSlice
             */
            endpoints: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpoints[];
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumEndpointSlice";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            /**
             * Namespace indicate as CiliumEndpointSlice namespace.
             * All the CiliumEndpoints within the same namespace are put together
             * in CiliumEndpointSlice.
             */
            namespace: string;
        }

        /**
         * CoreCiliumEndpoint is slim version of status of CiliumEndpoint.
         */
        export interface CiliumEndpointSliceEndpoints {
            encryption: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsEncryption;
            /**
             * IdentityID is the numeric identity of the endpoint
             */
            id: number;
            /**
             * Name indicate as CiliumEndpoint name.
             */
            name: string;
            /**
             * NamedPorts List of named Layer 4 port and protocol pairs which will be used in Network
             * Policy specs.
             *
             * swagger:model NamedPorts
             */
            "named-ports": outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNamedPorts[];
            networking: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNetworking;
        }

        /**
         * EncryptionSpec defines the encryption relevant configuration of a node.
         */
        export interface CiliumEndpointSliceEndpointsEncryption {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * EncryptionSpec defines the encryption relevant configuration of a node.
         */
        export interface CiliumEndpointSliceEndpointsEncryptionPatch {
            /**
             * Key is the index to the key to use for encryption or 0 if encryption is
             * disabled.
             */
            key: number;
        }

        /**
         * Port Layer 4 port / protocol pair
         *
         * swagger:model Port
         */
        export interface CiliumEndpointSliceEndpointsNamedPorts {
            /**
             * Optional layer 4 port name
             */
            name: string;
            /**
             * Layer 4 port number
             */
            port: number;
            /**
             * Layer 4 protocol
             * Enum: ["TCP","UDP","SCTP","ICMP","ICMPV6","ANY"]
             */
            protocol: string;
        }

        /**
         * Port Layer 4 port / protocol pair
         *
         * swagger:model Port
         */
        export interface CiliumEndpointSliceEndpointsNamedPortsPatch {
            /**
             * Optional layer 4 port name
             */
            name: string;
            /**
             * Layer 4 port number
             */
            port: number;
            /**
             * Layer 4 protocol
             * Enum: ["TCP","UDP","SCTP","ICMP","ICMPV6","ANY"]
             */
            protocol: string;
        }

        /**
         * EndpointNetworking is the addressing information of an endpoint.
         */
        export interface CiliumEndpointSliceEndpointsNetworking {
            /**
             * IP4/6 addresses assigned to this Endpoint
             */
            addressing: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNetworkingAddressing[];
            /**
             * NodeIP is the IP of the node the endpoint is running on. The IP must
             * be reachable between nodes.
             */
            node: string;
        }

        /**
         * AddressPair is a pair of IPv4 and/or IPv6 address.
         */
        export interface CiliumEndpointSliceEndpointsNetworkingAddressing {
            ipv4: string;
            ipv6: string;
        }

        /**
         * AddressPair is a pair of IPv4 and/or IPv6 address.
         */
        export interface CiliumEndpointSliceEndpointsNetworkingAddressingPatch {
            ipv4: string;
            ipv6: string;
        }

        /**
         * EndpointNetworking is the addressing information of an endpoint.
         */
        export interface CiliumEndpointSliceEndpointsNetworkingPatch {
            /**
             * IP4/6 addresses assigned to this Endpoint
             */
            addressing: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNetworkingAddressingPatch[];
            /**
             * NodeIP is the IP of the node the endpoint is running on. The IP must
             * be reachable between nodes.
             */
            node: string;
        }

        /**
         * CoreCiliumEndpoint is slim version of status of CiliumEndpoint.
         */
        export interface CiliumEndpointSliceEndpointsPatch {
            encryption: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsEncryptionPatch;
            /**
             * IdentityID is the numeric identity of the endpoint
             */
            id: number;
            /**
             * Name indicate as CiliumEndpoint name.
             */
            name: string;
            /**
             * NamedPorts List of named Layer 4 port and protocol pairs which will be used in Network
             * Policy specs.
             *
             * swagger:model NamedPorts
             */
            "named-ports": outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNamedPortsPatch[];
            networking: outputs.cilium.v2alpha1.CiliumEndpointSliceEndpointsNetworkingPatch;
        }

        /**
         * CiliumL2AnnouncementPolicy is a Kubernetes third-party resource which
         * is used to defined which nodes should announce what services on the
         * L2 network.
         */
        export interface CiliumL2AnnouncementPolicy {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumL2AnnouncementPolicy";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpec;
            status: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicyStatus;
        }

        /**
         * Spec is a human readable description of a L2 announcement policy
         */
        export interface CiliumL2AnnouncementPolicySpec {
            /**
             * If true, the external IPs of the services are announced
             */
            externalIPs: boolean;
            /**
             * A list of regular expressions that express which network interface(s) should be used
             * to announce the services over. If nil, all network interfaces are used.
             */
            interfaces: string[];
            /**
             * If true, the loadbalancer IPs of the services are announced
             *
             * If nil this policy applies to all services.
             */
            loadBalancerIPs: boolean;
            nodeSelector: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecNodeSelector;
            serviceSelector: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecServiceSelector;
        }

        /**
         * NodeSelector selects a group of nodes which will announce the IPs for
         * the services selected by the service selector.
         *
         * If nil this policy applies to all nodes.
         */
        export interface CiliumL2AnnouncementPolicySpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumL2AnnouncementPolicySpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumL2AnnouncementPolicySpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector selects a group of nodes which will announce the IPs for
         * the services selected by the service selector.
         *
         * If nil this policy applies to all nodes.
         */
        export interface CiliumL2AnnouncementPolicySpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is a human readable description of a L2 announcement policy
         */
        export interface CiliumL2AnnouncementPolicySpecPatch {
            /**
             * If true, the external IPs of the services are announced
             */
            externalIPs: boolean;
            /**
             * A list of regular expressions that express which network interface(s) should be used
             * to announce the services over. If nil, all network interfaces are used.
             */
            interfaces: string[];
            /**
             * If true, the loadbalancer IPs of the services are announced
             *
             * If nil this policy applies to all services.
             */
            loadBalancerIPs: boolean;
            nodeSelector: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecNodeSelectorPatch;
            serviceSelector: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecServiceSelectorPatch;
        }

        /**
         * ServiceSelector selects a set of services which will be announced over L2 networks.
         * The loadBalancerClass for a service must be nil or specify a supported class, e.g.
         * "io.cilium/l2-announcer". Refer to the following document for additional details
         * regarding load balancer classes:
         *
         *   https://kubernetes.io/docs/concepts/services-networking/service/#load-balancer-class
         *
         * If nil this policy applies to all services.
         */
        export interface CiliumL2AnnouncementPolicySpecServiceSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecServiceSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumL2AnnouncementPolicySpecServiceSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumL2AnnouncementPolicySpecServiceSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector selects a set of services which will be announced over L2 networks.
         * The loadBalancerClass for a service must be nil or specify a supported class, e.g.
         * "io.cilium/l2-announcer". Refer to the following document for additional details
         * regarding load balancer classes:
         *
         *   https://kubernetes.io/docs/concepts/services-networking/service/#load-balancer-class
         *
         * If nil this policy applies to all services.
         */
        export interface CiliumL2AnnouncementPolicySpecServiceSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicySpecServiceSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Status is the status of the policy.
         */
        export interface CiliumL2AnnouncementPolicyStatus {
            /**
             * Current service state
             */
            conditions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicyStatusConditions[];
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumL2AnnouncementPolicyStatusConditions {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumL2AnnouncementPolicyStatusConditionsPatch {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Status is the status of the policy.
         */
        export interface CiliumL2AnnouncementPolicyStatusPatch {
            /**
             * Current service state
             */
            conditions: outputs.cilium.v2alpha1.CiliumL2AnnouncementPolicyStatusConditionsPatch[];
        }

        /**
         * CiliumLoadBalancerIPPool is a Kubernetes third-party resource which
         * is used to defined pools of IPs which the operator can use to to allocate
         * and advertise IPs for Services of type LoadBalancer.
         */
        export interface CiliumLoadBalancerIPPool {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumLoadBalancerIPPool";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpec;
            status: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolStatus;
        }

        /**
         * Spec is a human readable description for a BGP load balancer
         * ip pool.
         */
        export interface CiliumLoadBalancerIPPoolSpec {
            /**
             * AllowFirstLastIPs, if set to `Yes` or undefined means that the first and last IPs of each CIDR will be allocatable.
             * If `No`, these IPs will be reserved. This field is ignored for /{31,32} and /{127,128} CIDRs since
             * reserving the first and last IPs would make the CIDRs unusable.
             */
            allowFirstLastIPs: string;
            /**
             * Blocks is a list of CIDRs comprising this IP Pool
             */
            blocks: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecBlocks[];
            /**
             * Disabled, if set to true means that no new IPs will be allocated from this pool.
             * Existing allocations will not be removed from services.
             */
            disabled: boolean;
            serviceSelector: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecServiceSelector;
        }

        /**
         * CiliumLoadBalancerIPPoolIPBlock describes a single IP block.
         */
        export interface CiliumLoadBalancerIPPoolSpecBlocks {
            cidr: string;
            start: string;
            stop: string;
        }

        /**
         * CiliumLoadBalancerIPPoolIPBlock describes a single IP block.
         */
        export interface CiliumLoadBalancerIPPoolSpecBlocksPatch {
            cidr: string;
            start: string;
            stop: string;
        }

        /**
         * Spec is a human readable description for a BGP load balancer
         * ip pool.
         */
        export interface CiliumLoadBalancerIPPoolSpecPatch {
            /**
             * AllowFirstLastIPs, if set to `Yes` or undefined means that the first and last IPs of each CIDR will be allocatable.
             * If `No`, these IPs will be reserved. This field is ignored for /{31,32} and /{127,128} CIDRs since
             * reserving the first and last IPs would make the CIDRs unusable.
             */
            allowFirstLastIPs: string;
            /**
             * Blocks is a list of CIDRs comprising this IP Pool
             */
            blocks: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecBlocksPatch[];
            /**
             * Disabled, if set to true means that no new IPs will be allocated from this pool.
             * Existing allocations will not be removed from services.
             */
            disabled: boolean;
            serviceSelector: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecServiceSelectorPatch;
        }

        /**
         * ServiceSelector selects a set of services which are eligible to receive IPs from this
         */
        export interface CiliumLoadBalancerIPPoolSpecServiceSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecServiceSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumLoadBalancerIPPoolSpecServiceSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumLoadBalancerIPPoolSpecServiceSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * ServiceSelector selects a set of services which are eligible to receive IPs from this
         */
        export interface CiliumLoadBalancerIPPoolSpecServiceSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolSpecServiceSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Status is the status of the IP Pool.
         *
         * It might be possible for users to define overlapping IP Pools, we can't validate or enforce non-overlapping pools
         * during object creation. The Cilium operator will do this validation and update the status to reflect the ability
         * to allocate IPs from this pool.
         */
        export interface CiliumLoadBalancerIPPoolStatus {
            /**
             * Current service state
             */
            conditions: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolStatusConditions[];
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumLoadBalancerIPPoolStatusConditions {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Condition contains details for one aspect of the current state of this API Resource.
         */
        export interface CiliumLoadBalancerIPPoolStatusConditionsPatch {
            /**
             * lastTransitionTime is the last time the condition transitioned from one status to another.
             * This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
             */
            lastTransitionTime: string;
            /**
             * message is a human readable message indicating details about the transition.
             * This may be an empty string.
             */
            message: string;
            /**
             * observedGeneration represents the .metadata.generation that the condition was set based upon.
             * For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
             * with respect to the current state of the instance.
             */
            observedGeneration: number;
            /**
             * reason contains a programmatic identifier indicating the reason for the condition's last transition.
             * Producers of specific condition types may define expected values and meanings for this field,
             * and whether the values are considered a guaranteed API.
             * The value should be a CamelCase string.
             * This field may not be empty.
             */
            reason: string;
            /**
             * status of the condition, one of True, False, Unknown.
             */
            status: string;
            /**
             * type of condition in CamelCase or in foo.example.com/CamelCase.
             */
            type: string;
        }

        /**
         * Status is the status of the IP Pool.
         *
         * It might be possible for users to define overlapping IP Pools, we can't validate or enforce non-overlapping pools
         * during object creation. The Cilium operator will do this validation and update the status to reflect the ability
         * to allocate IPs from this pool.
         */
        export interface CiliumLoadBalancerIPPoolStatusPatch {
            /**
             * Current service state
             */
            conditions: outputs.cilium.v2alpha1.CiliumLoadBalancerIPPoolStatusConditionsPatch[];
        }

        /**
         * CiliumNodeConfig is a list of configuration key-value pairs. It is applied to
         * nodes indicated by a label selector.
         *
         * If multiple overrides apply to the same node, they will be ordered by name
         * with later Overrides overwriting any conflicting keys.
         */
        export interface CiliumNodeConfig {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumNodeConfig";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumNodeConfigSpec;
        }

        /**
         * Spec is the desired Cilium configuration overrides for a given node
         */
        export interface CiliumNodeConfigSpec {
            /**
             * Defaults is treated the same as the cilium-config ConfigMap - a set
             * of key-value pairs parsed by the agent and operator processes.
             * Each key must be a valid config-map data field (i.e. a-z, A-Z, -, _, and .)
             */
            defaults: {[key: string]: string};
            nodeSelector: outputs.cilium.v2alpha1.CiliumNodeConfigSpecNodeSelector;
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If not supplied, then this config applies to no nodes. If
         * empty, then it applies to all nodes.
         */
        export interface CiliumNodeConfigSpecNodeSelector {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumNodeConfigSpecNodeSelectorMatchExpressions[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNodeConfigSpecNodeSelectorMatchExpressions {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * A label selector requirement is a selector that contains values, a key, and an operator that
         * relates the key and values.
         */
        export interface CiliumNodeConfigSpecNodeSelectorMatchExpressionsPatch {
            /**
             * key is the label key that the selector applies to.
             */
            key: string;
            /**
             * operator represents a key's relationship to a set of values.
             * Valid operators are In, NotIn, Exists and DoesNotExist.
             */
            operator: string;
            /**
             * values is an array of string values. If the operator is In or NotIn,
             * the values array must be non-empty. If the operator is Exists or DoesNotExist,
             * the values array must be empty. This array is replaced during a strategic
             * merge patch.
             */
            values: string[];
        }

        /**
         * NodeSelector is a label selector that determines to which nodes
         * this configuration applies.
         * If not supplied, then this config applies to no nodes. If
         * empty, then it applies to all nodes.
         */
        export interface CiliumNodeConfigSpecNodeSelectorPatch {
            /**
             * matchExpressions is a list of label selector requirements. The requirements are ANDed.
             */
            matchExpressions: outputs.cilium.v2alpha1.CiliumNodeConfigSpecNodeSelectorMatchExpressionsPatch[];
            /**
             * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
             * map is equivalent to an element of matchExpressions, whose key field is "key", the
             * operator is "In", and the values array contains only "value". The requirements are ANDed.
             */
            matchLabels: {[key: string]: string};
        }

        /**
         * Spec is the desired Cilium configuration overrides for a given node
         */
        export interface CiliumNodeConfigSpecPatch {
            /**
             * Defaults is treated the same as the cilium-config ConfigMap - a set
             * of key-value pairs parsed by the agent and operator processes.
             * Each key must be a valid config-map data field (i.e. a-z, A-Z, -, _, and .)
             */
            defaults: {[key: string]: string};
            nodeSelector: outputs.cilium.v2alpha1.CiliumNodeConfigSpecNodeSelectorPatch;
        }

        /**
         * CiliumPodIPPool defines an IP pool that can be used for pooled IPAM (i.e. the multi-pool IPAM
         * mode).
         */
        export interface CiliumPodIPPool {
            /**
             * APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
             */
            apiVersion: "cilium.io/v2alpha1";
            /**
             * Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: "CiliumPodIPPool";
            /**
             * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            metadata: outputs.meta.v1.ObjectMeta;
            spec: outputs.cilium.v2alpha1.CiliumPodIPPoolSpec;
        }

        export interface CiliumPodIPPoolSpec {
            ipv4: outputs.cilium.v2alpha1.CiliumPodIPPoolSpecIpv4;
            ipv6: outputs.cilium.v2alpha1.CiliumPodIPPoolSpecIpv6;
        }

        /**
         * IPv4 specifies the IPv4 CIDRs and mask sizes of the pool
         */
        export interface CiliumPodIPPoolSpecIpv4 {
            /**
             * CIDRs is a list of IPv4 CIDRs that are part of the pool.
             */
            cidrs: string[];
            /**
             * MaskSize is the mask size of the pool.
             */
            maskSize: number;
        }

        /**
         * IPv4 specifies the IPv4 CIDRs and mask sizes of the pool
         */
        export interface CiliumPodIPPoolSpecIpv4Patch {
            /**
             * CIDRs is a list of IPv4 CIDRs that are part of the pool.
             */
            cidrs: string[];
            /**
             * MaskSize is the mask size of the pool.
             */
            maskSize: number;
        }

        /**
         * IPv6 specifies the IPv6 CIDRs and mask sizes of the pool
         */
        export interface CiliumPodIPPoolSpecIpv6 {
            /**
             * CIDRs is a list of IPv6 CIDRs that are part of the pool.
             */
            cidrs: string[];
            /**
             * MaskSize is the mask size of the pool.
             */
            maskSize: number;
        }

        /**
         * IPv6 specifies the IPv6 CIDRs and mask sizes of the pool
         */
        export interface CiliumPodIPPoolSpecIpv6Patch {
            /**
             * CIDRs is a list of IPv6 CIDRs that are part of the pool.
             */
            cidrs: string[];
            /**
             * MaskSize is the mask size of the pool.
             */
            maskSize: number;
        }

        export interface CiliumPodIPPoolSpecPatch {
            ipv4: outputs.cilium.v2alpha1.CiliumPodIPPoolSpecIpv4Patch;
            ipv6: outputs.cilium.v2alpha1.CiliumPodIPPoolSpecIpv6Patch;
        }

    }
}

export namespace meta {
    export namespace v1 {
        /**
         * ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of {ObjectMeta, ListMeta}.
         */
        export interface ListMeta {
            /**
             * continue may be set if the user set a limit on the number of items returned, and indicates that the server has more data available. The value is opaque and may be used to issue another request to the endpoint that served this list to retrieve the next set of available objects. Continuing a consistent list may not be possible if the server configuration has changed or more than a few minutes have passed. The resourceVersion field returned when using this continue value will be identical to the value in the first response, unless you have received this token from an error message.
             */
            continue: string;
            /**
             * remainingItemCount is the number of subsequent items in the list which are not included in this list response. If the list request contained label or field selectors, then the number of remaining items is unknown and the field will be left unset and omitted during serialization. If the list is complete (either because it is not chunking or because this is the last chunk), then there are no more remaining items and this field will be left unset and omitted during serialization. Servers older than v1.15 do not set this field. The intended use of the remainingItemCount is *estimating* the size of a collection. Clients should not rely on the remainingItemCount to be set or to be exact.
             */
            remainingItemCount: number;
            /**
             * String that identifies the server's internal version of this object that can be used by clients to determine when objects have changed. Value must be treated as opaque by clients and passed unmodified back to the server. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
             */
            resourceVersion: string;
            /**
             * Deprecated: selfLink is a legacy read-only field that is no longer populated by the system.
             */
            selfLink: string;
        }

        /**
         * ManagedFieldsEntry is a workflow-id, a FieldSet and the group version of the resource that the fieldset applies to.
         */
        export interface ManagedFieldsEntry {
            /**
             * APIVersion defines the version of this resource that this field set applies to. The format is "group/version" just like the top-level APIVersion field. It is necessary to track the version of a field set because it cannot be automatically converted.
             */
            apiVersion: string;
            /**
             * FieldsType is the discriminator for the different fields format and version. There is currently only one possible value: "FieldsV1"
             */
            fieldsType: string;
            /**
             * FieldsV1 holds the first JSON version format as described in the "FieldsV1" type.
             */
            fieldsV1: any;
            /**
             * Manager is an identifier of the workflow managing these fields.
             */
            manager: string;
            /**
             * Operation is the type of operation which lead to this ManagedFieldsEntry being created. The only valid values for this field are 'Apply' and 'Update'.
             */
            operation: string;
            /**
             * Subresource is the name of the subresource used to update that object, or empty string if the object was updated through the main resource. The value of this field is used to distinguish between managers, even if they share the same name. For example, a status update will be distinct from a regular update using the same manager name. Note that the APIVersion field is not related to the Subresource field and it always corresponds to the version of the main resource.
             */
            subresource: string;
            /**
             * Time is the timestamp of when the ManagedFields entry was added. The timestamp will also be updated if a field is added, the manager changes any of the owned fields value or removes a field. The timestamp does not update when a field is removed from the entry because another manager took it over.
             */
            time: string;
        }

        /**
         * ManagedFieldsEntry is a workflow-id, a FieldSet and the group version of the resource that the fieldset applies to.
         */
        export interface ManagedFieldsEntryPatch {
            /**
             * APIVersion defines the version of this resource that this field set applies to. The format is "group/version" just like the top-level APIVersion field. It is necessary to track the version of a field set because it cannot be automatically converted.
             */
            apiVersion: string;
            /**
             * FieldsType is the discriminator for the different fields format and version. There is currently only one possible value: "FieldsV1"
             */
            fieldsType: string;
            /**
             * FieldsV1 holds the first JSON version format as described in the "FieldsV1" type.
             */
            fieldsV1: any;
            /**
             * Manager is an identifier of the workflow managing these fields.
             */
            manager: string;
            /**
             * Operation is the type of operation which lead to this ManagedFieldsEntry being created. The only valid values for this field are 'Apply' and 'Update'.
             */
            operation: string;
            /**
             * Subresource is the name of the subresource used to update that object, or empty string if the object was updated through the main resource. The value of this field is used to distinguish between managers, even if they share the same name. For example, a status update will be distinct from a regular update using the same manager name. Note that the APIVersion field is not related to the Subresource field and it always corresponds to the version of the main resource.
             */
            subresource: string;
            /**
             * Time is the timestamp of when the ManagedFields entry was added. The timestamp will also be updated if a field is added, the manager changes any of the owned fields value or removes a field. The timestamp does not update when a field is removed from the entry because another manager took it over.
             */
            time: string;
        }

        /**
         * ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create.
         */
        export interface ObjectMeta {
            /**
             * Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations
             */
            annotations: {[key: string]: string};
            /**
             * CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC.
             *
             * Populated by the system. Read-only. Null for lists. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            creationTimestamp: string;
            /**
             * Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only.
             */
            deletionGracePeriodSeconds: number;
            /**
             * DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This field is set by the server when a graceful deletion is requested by the user, and is not directly settable by a client. The resource is expected to be deleted (no longer visible from resource lists, and not reachable by name) after the time in this field, once the finalizers list is empty. As long as the finalizers list contains items, deletion is blocked. Once the deletionTimestamp is set, this value may not be unset or be set further into the future, although it may be shortened or the resource may be deleted prior to this time. For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react by sending a graceful termination signal to the containers in the pod. After that 30 seconds, the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup, remove the pod from the API. In the presence of network partitions, this object may still exist after this timestamp, until an administrator or automated process can determine the resource is fully terminated. If not set, graceful deletion of the object has not been requested.
             *
             * Populated by the system when a graceful deletion is requested. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            deletionTimestamp: string;
            /**
             * Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order.  Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list.
             */
            finalizers: string[];
            /**
             * GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server.
             *
             * If this field is specified and the generated name exists, the server will return a 409.
             *
             * Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency
             */
            generateName: string;
            /**
             * A sequence number representing a specific generation of the desired state. Populated by the system. Read-only.
             */
            generation: number;
            /**
             * Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels
             */
            labels: {[key: string]: string};
            /**
             * ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object.
             */
            managedFields: outputs.meta.v1.ManagedFieldsEntry[];
            /**
             * Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
             */
            name: string;
            /**
             * Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty.
             *
             * Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces
             */
            namespace: string;
            /**
             * List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller.
             */
            ownerReferences: outputs.meta.v1.OwnerReference[];
            /**
             * An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources.
             *
             * Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
             */
            resourceVersion: string;
            /**
             * Deprecated: selfLink is a legacy read-only field that is no longer populated by the system.
             */
            selfLink: string;
            /**
             * UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations.
             *
             * Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             */
            uid: string;
        }

        /**
         * ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create.
         */
        export interface ObjectMetaPatch {
            /**
             * Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations
             */
            annotations: {[key: string]: string};
            /**
             * CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC.
             *
             * Populated by the system. Read-only. Null for lists. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            creationTimestamp: string;
            /**
             * Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only.
             */
            deletionGracePeriodSeconds: number;
            /**
             * DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This field is set by the server when a graceful deletion is requested by the user, and is not directly settable by a client. The resource is expected to be deleted (no longer visible from resource lists, and not reachable by name) after the time in this field, once the finalizers list is empty. As long as the finalizers list contains items, deletion is blocked. Once the deletionTimestamp is set, this value may not be unset or be set further into the future, although it may be shortened or the resource may be deleted prior to this time. For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react by sending a graceful termination signal to the containers in the pod. After that 30 seconds, the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup, remove the pod from the API. In the presence of network partitions, this object may still exist after this timestamp, until an administrator or automated process can determine the resource is fully terminated. If not set, graceful deletion of the object has not been requested.
             *
             * Populated by the system when a graceful deletion is requested. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
             */
            deletionTimestamp: string;
            /**
             * Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order.  Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list.
             */
            finalizers: string[];
            /**
             * GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server.
             *
             * If this field is specified and the generated name exists, the server will return a 409.
             *
             * Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency
             */
            generateName: string;
            /**
             * A sequence number representing a specific generation of the desired state. Populated by the system. Read-only.
             */
            generation: number;
            /**
             * Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels
             */
            labels: {[key: string]: string};
            /**
             * ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object.
             */
            managedFields: outputs.meta.v1.ManagedFieldsEntryPatch[];
            /**
             * Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
             */
            name: string;
            /**
             * Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty.
             *
             * Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces
             */
            namespace: string;
            /**
             * List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller.
             */
            ownerReferences: outputs.meta.v1.OwnerReferencePatch[];
            /**
             * An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources.
             *
             * Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
             */
            resourceVersion: string;
            /**
             * Deprecated: selfLink is a legacy read-only field that is no longer populated by the system.
             */
            selfLink: string;
            /**
             * UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations.
             *
             * Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             */
            uid: string;
        }

        /**
         * OwnerReference contains enough information to let you identify an owning object. An owning object must be in the same namespace as the dependent, or be cluster-scoped, so there is no namespace field.
         */
        export interface OwnerReference {
            /**
             * API version of the referent.
             */
            apiVersion: string;
            /**
             * If true, AND if the owner has the "foregroundDeletion" finalizer, then the owner cannot be deleted from the key-value store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion for how the garbage collector interacts with this field and enforces the foreground deletion. Defaults to false. To set this field, a user needs "delete" permission of the owner, otherwise 422 (Unprocessable Entity) will be returned.
             */
            blockOwnerDeletion: boolean;
            /**
             * If true, this reference points to the managing controller.
             */
            controller: boolean;
            /**
             * Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            /**
             * Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
             */
            name: string;
            /**
             * UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             */
            uid: string;
        }

        /**
         * OwnerReference contains enough information to let you identify an owning object. An owning object must be in the same namespace as the dependent, or be cluster-scoped, so there is no namespace field.
         */
        export interface OwnerReferencePatch {
            /**
             * API version of the referent.
             */
            apiVersion: string;
            /**
             * If true, AND if the owner has the "foregroundDeletion" finalizer, then the owner cannot be deleted from the key-value store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion for how the garbage collector interacts with this field and enforces the foreground deletion. Defaults to false. To set this field, a user needs "delete" permission of the owner, otherwise 422 (Unprocessable Entity) will be returned.
             */
            blockOwnerDeletion: boolean;
            /**
             * If true, this reference points to the managing controller.
             */
            controller: boolean;
            /**
             * Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
             */
            kind: string;
            /**
             * Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
             */
            name: string;
            /**
             * UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
             */
            uid: string;
        }

    }
}
