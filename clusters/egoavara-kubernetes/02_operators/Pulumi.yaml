name: ek-operators
description: "Phase 2: Operators (CRD + Operator only)"
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
    nodeargs: "--loader ts-node/esm --no-warnings"
config:
  pulumi:tags:
    value:
      pulumi:template: helm-kubernetes-typescript
