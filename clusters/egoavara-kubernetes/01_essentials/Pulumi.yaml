name: ek-essentials
description: "Phase 1: Core infrastructure (Istio, Cilium BGP, cert-manager, Rook Ceph, etc.)"
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
    nodeargs: "--loader ts-node/esm --no-warnings"
config:
  pulumi:tags:
    value:
      pulumi:template: helm-kubernetes-typescript
