name: ek-persistence
description: "Phase 5: Data infrastructure (PostgreSQL, Qdrant, Valkey, NATS, etcd)"
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
    nodeargs: "--loader ts-node/esm --no-warnings"
config:
  pulumi:tags:
    value:
      pulumi:template: helm-kubernetes-typescript
