name: ek-auth
description: "Phase 4: Auth layer (Zitadel, SpiceDB, oauth2-proxy, Private Gateway)"
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
    nodeargs: "--loader ts-node/esm --no-warnings"
config:
  pulumi:tags:
    value:
      pulumi:template: helm-kubernetes-typescript
